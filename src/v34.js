const VERSION='3.4';
const PIN='7900';
const KEY='poolside-pulse-v34-local';
const DEFAULT_PLAYLIST='https://suno.com/playlist/cf4b536e-9005-4c98-9ea5-a7f01eca116f';
const DEFAULT_ADDRESS='615 Serenity Shores Ln, Kimberling City, MO 65686';
const DEFAULT_TRACKS=[{title:'Load or import the Serenity Shores playlist',artist:'Poolside Pulse',duration:'3:00',audioUrl:'',sourceUrl:''}];
const BASE={admin:false,screen:'home',tab:'station',sync:false,syncMode:'starting',syncNote:'Connecting station control...',revision:0,updatedAt:0,platform:'suno',playlistName:'Serenity Shores Poolside Pulse',playlistUrl:DEFAULT_PLAYLIST,tracks:DEFAULT_TRACKS,current:0,stationIntent:'stopped',command:null,announcement:null,lastError:'',voiceMode:'ai',aiVoice:'marin',deviceVoice:'',voiceRate:.94,voicePitch:1,poolOpen:'09:00',poolClose:'21:00',autoStart:true,autoStop:true,address:DEFAULT_ADDRESS,lat:'',lon:'',radius:10,locationNote:'Serenity Shores address is ready to verify. Device GPS at the resort is best.',weather:'Weather monitor is standing by.',instant:'Attention guests, this is a Serenity Shores pool announcement.',owner:'Welcome to Serenity Shores. We hope your family has an unforgettable day at the lake and pool.',manager:'Pool reminder: children must be supervised, glass is not permitted, and safety comes first.',birthdayName:'',birthdayText:'Happy birthday from Serenity Shores. We hope your day is absolutely wonderful.',regular:[{id:'close15',label:'Closing in 15 minutes',time:'20:45',enabled:true,text:'Attention guests, the pool will close in 15 minutes. Please begin gathering your belongings.'},{id:'close5',label:'Closing in 5 minutes',time:'20:55',enabled:true,text:'Attention guests, the pool will close in 5 minutes. Thank you for spending the day at Serenity Shores.'}],oneTime:[],log:[]};
let S=load();let voices=[];let music=new Audio();let announcing=false;let lastCommandId='';let lastAnnouncementId='';let lastScheduleKey='';let armReady=false;let importing=false;
music.preload='auto';
music.onended=()=>advance(1,false);
music.onerror=()=>{S.lastError='Audio failed for this track. Skipping.';advance(1,true)};
function clone(o){return JSON.parse(JSON.stringify(o))}
function clean(x){let s={...clone(BASE),...(x||{})};s.tracks=Array.isArray(s.tracks)&&s.tracks.length?s.tracks:clone(DEFAULT_TRACKS);s.regular=Array.isArray(s.regular)?s.regular:clone(BASE.regular);s.oneTime=Array.isArray(s.oneTime)?s.oneTime:[];s.log=Array.isArray(s.log)?s.log:[];s.current=Math.min(Math.max(Number(s.current)||0,0),Math.max(s.tracks.length-1,0));return s}
function load(){try{return clean(JSON.parse(localStorage.getItem(KEY)||'{}'))}catch{return clean({})}}
function local(){localStorage.setItem(KEY,JSON.stringify(S))}
function publicState(){let keep=clone(S);keep.admin=false;keep.screen='home';keep.tab='station';return keep}
function now(){return new Date().toLocaleTimeString([], {hour:'numeric',minute:'2-digit'})}
function log(msg){S.log=[[msg,now()],...(S.log||[])].slice(0,60);local()}
function esc(x){return String(x??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function $(id){return document.getElementById(id)}
function val(id){return $(id)?.value||''}
function track(){return S.tracks[S.current]||DEFAULT_TRACKS[0]}
function audioTracks(){return (S.tracks||[]).filter(t=>t.audioUrl)}
function minutes(hhmm){let [h,m]=String(hhmm||'0:0').split(':').map(Number);return (h||0)*60+(m||0)}
function inPoolHours(){let n=new Date(),m=n.getHours()*60+n.getMinutes(),a=minutes(S.poolOpen),b=minutes(S.poolClose);return a<=b?m>=a&&m<b:m>=a||m<b}
async function save(push=true){local(); if(push) await pushState(); render()}
async function pushState(){try{let r=await fetch('/api/state',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({state:publicState()})});let d=await r.json();S.sync=!!d.cloudSync;S.syncMode=d.syncMode||'server';S.syncNote=d.note||'Shared station control active.';if(d.state){S.revision=d.state.revision||S.revision;S.updatedAt=d.state.savedAt||S.updatedAt}local();return d}catch(e){S.sync=false;S.syncMode='local';S.syncNote='Shared control unavailable: '+e.message;local();return null}}
async function pullState(){try{let r=await fetch('/api/state',{cache:'no-store'});let d=await r.json();S.sync=!!d.cloudSync;S.syncMode=d.syncMode||'server';S.syncNote=d.note||'Shared station control active.';if(d.state&&Number(d.state.revision||0)>Number(S.revision||0)){let keep={admin:S.admin,screen:S.screen,tab:S.tab,lastError:S.lastError};S=clean({...S,...d.state,...keep});local();await applyRemote();render()}else{local(); updateSyncPill()}}catch(e){S.sync=false;S.syncMode='local';S.syncNote='Shared control temporarily unavailable.';updateSyncPill()}}
function updateSyncPill(){let el=$('syncPill'); if(el) el.textContent=syncText()}
function syncText(){return S.sync?`Sync Active · ${S.syncMode}`:'Local/preview sync'}
async function ensurePlaylistLoaded(){if(importing)return false;if(audioTracks().length)return true; if(!S.playlistUrl)return false; importing=true; S.lastError='Importing playlist audio...'; render(); try{let d=await fetchJson('/api/suno-playlist?url='+encodeURIComponent(S.playlistUrl)); if(d.tracks?.length){S.tracks=d.tracks.map(t=>({title:t.title||'Untitled',artist:t.artist||'Suno',duration:t.duration||'3:00',audioUrl:t.audioUrl||'',sourceUrl:t.sourceUrl||'',imageUrl:t.imageUrl||''})); if(d.playlistName)S.playlistName=d.playlistName; S.current=0; S.lastError=d.audioWarning||''; log('Imported '+S.tracks.length+' Suno tracks'); await save(true); importing=false; return audioTracks().length>0} throw Error(d.error||'No tracks found')}catch(e){S.lastError='Playlist import failed: '+e.message; importing=false; local(); render(); return false}}
async function fetchJson(url,opts){let r=await fetch(url,opts);let d=await r.json().catch(()=>({})); if(!r.ok||d.ok===false) throw Error(d.error||`HTTP ${r.status}`); return d}
async function play(push=true){armReady=true;let ok=await ensurePlaylistLoaded(); if(!ok){S.stationIntent='stopped';S.lastError='No playable audio is loaded yet. Import the Suno playlist in Command Center.';await save(push);return}let t=track(); if(!t.audioUrl){let idx=S.tracks.findIndex(x=>x.audioUrl); if(idx>=0){S.current=idx;t=track()}else{return}} music.src=t.audioUrl; music.volume=.95; try{await music.play();S.stationIntent='playing';S.lastError='';log('Playing '+t.title)}catch(e){S.stationIntent='stopped';S.lastError='This browser blocked playback. Tap Arm / Play Station once on the sound-system device.';log('Playback blocked')} if(push){S.command={id:Date.now()+':play',type:'play'};await save(true)}else{local();render()}}
async function pause(push=true){music.pause();S.stationIntent='paused'; if(push){S.command={id:Date.now()+':pause',type:'pause'};await save(true)}else{local();render()}}
async function stop(push=true){music.pause();music.currentTime=0;S.stationIntent='stopped'; if(push){S.command={id:Date.now()+':stop',type:'stop'};await save(true)}else{local();render()}}
async function advance(n=1,push=true){S.current=(S.current+n+S.tracks.length)%S.tracks.length;if(S.stationIntent==='playing'){if(push)S.command={id:Date.now()+':skip',type:'skip',n};await save(push);await play(false)}else{if(push){S.command={id:Date.now()+':skip',type:'skip',n};await save(true)}else{local();render()}}}
function prepAnnouncement(){let resume=S.stationIntent==='playing'&&!music.paused; if(resume)music.pause(); announcing=true; return resume}
function finishAnnouncement(resume, hold){announcing=false;if(resume&&!hold&&inPoolHours())setTimeout(()=>play(false),700);else render()}
async function speak(text, opts={}){let msg=String(text||'').trim();if(!msg)return;let resume=prepAnnouncement();let hold=!!opts.hold;let usedAi=false;
 if(S.voiceMode==='ai'){try{let r=await fetch('/api/tts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text:msg,voice:S.aiVoice||'marin',instructions:'Speak like a calm, polished, realistic resort announcer at Serenity Shores. Use clear public-address diction. For safety, sound firm and serious without panic.'})}); if(r.ok){let blob=await r.blob();let url=URL.createObjectURL(blob);let a=new Audio(url);a.volume=1;a.onended=()=>{URL.revokeObjectURL(url);finishAnnouncement(resume,hold)};await a.play();usedAi=true;log('AI voice announcement played');return}else{let d=await r.json().catch(()=>({}));S.lastError=`AI voice unavailable (${r.status}). Device voice fallback used.`;console.warn('TTS failed',r.status,d)}}catch(e){S.lastError='AI voice failed. Device voice fallback used.';console.warn(e)}}
 if(!usedAi){if(!('speechSynthesis'in window)){finishAnnouncement(resume,hold);return}speechSynthesis.cancel();let u=new SpeechSynthesisUtterance(msg);let v=voices.find(v=>v.name===S.deviceVoice)||bestDeviceVoices()[0]; if(v)u.voice=v;u.rate=Number(S.voiceRate)||.94;u.pitch=Number(S.voicePitch)||1;u.onend=()=>finishAnnouncement(resume,hold);speechSynthesis.speak(u);log('Device voice announcement played')}}
async function sendAnnouncement(text,hold=false){let msg=String(text||'').trim();if(!msg)return;S.announcement={id:Date.now()+':'+Math.random().toString(36).slice(2),text:msg,hold,createdAt:Date.now()};log('Announcement sent');await save(true); if(S.screen==='home')speak(msg,{hold})}
async function applyRemote(){let cmd=S.command;if(cmd?.id&&cmd.id!==lastCommandId&&!announcing){lastCommandId=cmd.id;if(cmd.type==='play')await play(false);if(cmd.type==='pause')await pause(false);if(cmd.type==='stop')await stop(false);if(cmd.type==='skip')await advance(cmd.n||1,false)}let a=S.announcement;if(a?.id&&a.id!==lastAnnouncementId){lastAnnouncementId=a.id; await speak(a.text,{hold:a.hold})}}
function bestDeviceVoices(){return voices.filter(v=>/^en/i.test(v.lang||'')).sort((a,b)=>voiceScore(b)-voiceScore(a)).slice(0,14)}
function voiceScore(v){let n=(v.name+' '+v.lang).toLowerCase(),s=0;['samantha','ava','allison','siri','premium','enhanced','natural','google','microsoft','aria','jenny'].forEach((p,i)=>{if(n.includes(p))s+=40-i});if(n.includes('en-us'))s+=10;return s}
async function importPlaylist(){S.platform=val('platform');S.playlistName=val('playlistName')||S.playlistName;S.playlistUrl=val('playlistUrl')||S.playlistUrl; if(S.platform!=='suno'){S.lastError='Saved as a reference. Automatic playable import currently works for Suno; use Bulk Import for other platforms until their official APIs are connected.';await save(true);return} await ensurePlaylistLoaded(); await save(true)}
async function bulkImport(){let rows=val('bulk').split('\n').map(x=>x.trim()).filter(Boolean); if(!rows.length)return; S.tracks=rows.map(r=>{let p=r.split('|').map(x=>x.trim());return{title:p[0]||'Untitled',artist:p[1]||'Unknown',duration:p[2]||'3:00',audioUrl:p[3]||'',sourceUrl:p[4]||''}});S.current=0;S.lastError='Bulk import saved and published.';await save(true)}
async function verify(){S.address=val('address')||S.address;try{let d=await fetchJson('/api/geocode?address='+encodeURIComponent(S.address));S.lat=String(d.latitude);S.lon=String(d.longitude);S.locationNote=(d.fallback?'Best available match. ':'Verified: ')+(d.matchedAddress||S.address);S.weather='Location ready. Weather monitoring can run.';await save(true)}catch(e){S.locationNote='Address lookup failed. Use Device GPS while physically at Serenity Shores, or enter coordinates manually.';S.lastError=e.message;await save(true)}}
function deviceLoc(){navigator.geolocation?.getCurrentPosition(async p=>{S.lat=p.coords.latitude.toFixed(6);S.lon=p.coords.longitude.toFixed(6);S.locationNote='Device GPS captured with about '+Math.round(p.coords.accuracy||0)+'m accuracy.';await save(true)},async()=>{S.locationNote='Device GPS permission failed.';await save(true)},{enableHighAccuracy:true,timeout:12000})}
async function weatherCheck(){if(!S.lat||!S.lon){S.weather='Verify location first.';render();return}try{let d=await fetchJson(`/api/weather?lat=${S.lat}&lon=${S.lon}&radiusMiles=${Number(S.radius)||10}`);S.weather=d.summary||'Weather checked.';if(d.threat){await sendAnnouncement('Attention guests. Weather activity has been detected nearby. Please exit the pool area and move indoors immediately.',true);S.stationIntent='paused'}await save(true)}catch(e){S.weather='Weather check failed: '+e.message;await save(true)}}
function scheduleKey(id){let d=new Date();return `${d.getFullYear()}-${d.getMonth()+1}-${d.getDate()}:${id}`}
async function scheduleTick(){let within=inPoolHours(); if(S.screen==='home'&&armReady&&!announcing){if(S.autoStart&&within&&S.stationIntent!=='playing')await play(false); if(S.autoStop&&!within&&S.stationIntent==='playing')await stop(false)} let n=new Date();let hm=String(n.getHours()).padStart(2,'0')+':'+String(n.getMinutes()).padStart(2,'0'); for(let r of S.regular||[]){if(r.enabled&&r.time===hm){let key=scheduleKey(r.id||r.label||r.time); if(key!==lastScheduleKey){lastScheduleKey=key;await sendAnnouncement(r.text,false)}}}}
function head(){return `<header class="top"><div class="brand"><b>SS</b><span>Serenity Shores<small>Poolside Pulse · V${VERSION}</small></span></div><div class="tog"><button id="homeBtn" class="${S.screen==='home'?'active':''}">Home</button><button id="cmdBtn" class="${S.screen!=='home'?'active':''}">Command</button></div></header>`}
function home(){let t=track(), armed=armReady?'Receiver Armed':'Tap once to arm receiver';return `${head()}<main class="home"><section class="hero"><div><p class="tag">PRIVATE RESORT STATION · V${VERSION}</p><h1>Poolside Pulse</h1><p class="sub">A command-controlled resort station for music, announcements, birthdays, daily messages, and weather-aware pool safety.</p><div class="actions"><button id="playHome">${S.stationIntent==='playing'?'Pause':'Arm / Play Station'}</button><button id="skipHome" class="secondary">Skip</button><button id="stopHome" class="secondary">Stop</button></div><p class="mini">${armed} · ${syncText()}</p></div><aside><b>Pool Hours</b><p>${esc(S.poolOpen)} – ${esc(S.poolClose)}</p><b>Weather Monitor</b><p>${esc(S.weather)}</p></aside></section><section class="now"><div class="art">SS<span>Pulse</span></div><div><p class="tag teal">${S.stationIntent==='playing'?'NOW PLAYING':'STATION READY'}</p><h2>${esc(t.title)}</h2><p>${esc(t.artist)} · ${esc(t.duration)}</p><div class="bar"><i></i></div><p>${esc(S.playlistName)}</p>${S.lastError?`<p class="warn">${esc(S.lastError)}</p>`:''}</div></section><section class="cards"><div class="card"><h3>Up Next</h3>${S.tracks.slice(S.current+1,S.current+5).map(x=>`<p class="line"><b>${esc(x.title)}</b><span>${esc(x.artist)}</span></p>`).join('')||'<p>Import or bulk-load a queue.</p>'}</div><div class="card"><h3>Daily Messages</h3><p>${esc(S.owner)}</p><p>${esc(S.manager)}</p></div></section></main>`}
function login(){return `${head()}<main class="wrap"><section class="card login"><h1>Command Center</h1><p>PIN protected station controls.</p><input id="pin" inputmode="numeric" type="password" placeholder="PIN"><button id="login">Enter</button><p class="mini">V${VERSION}</p></section></main>`}
function tabs(){let a=[['station','Station'],['announce','Announce'],['daily','Daily'],['hours','Hours'],['weather','Weather'],['voice','Voice']];return `<nav class="tabs">${a.map(x=>`<button data-tab="${x[0]}" class="${S.tab===x[0]?'on':''}">${x[1]}</button>`).join('')}</nav>`}
function command(body){return `${head()}<main class="wrap"><div class="version" id="syncPill">Command Center · Version ${VERSION} · ${syncText()}</div>${tabs()}${S.lastError?`<div class="status"><b>Status:</b> ${esc(S.lastError)}</div>`:''}${body}</main>`}
function station(){let q=S.tracks.map((t,i)=>`<div class="q ${i===S.current?'cur':''}"><div><b>${i+1}. ${esc(t.title)}</b><span>${esc(t.artist)} · ${esc(t.duration)} ${t.audioUrl?'· playable':'· title only'}</span></div><button data-song="${i}">Play</button></div>`).join('');return command(`<section class="panel"><h1>Station Source</h1><p>The playlist last imported here is published to every Home receiver until you replace it.</p><label>Platform<select id="platform"><option value="suno">Suno automatic playable import</option><option value="youtube">YouTube reference/manual</option><option value="spotify">Spotify reference/manual</option><option value="amazon">Amazon reference/manual</option><option value="google">Google/YouTube Music reference/manual</option></select></label><label>Playlist Name<input id="playlistName" value="${esc(S.playlistName)}"></label><label>Playlist URL<input id="playlistUrl" value="${esc(S.playlistUrl)}"></label><div class="actions"><button id="import">Import & Publish</button><button id="playCmd" class="secondary">Play All Receivers</button><button id="pauseCmd" class="secondary">Pause</button><button id="skipCmd" class="secondary">Skip</button><button id="stopCmd" class="secondary">Stop</button></div></section><section class="panel"><h2>Published Queue</h2>${q}</section><section class="panel"><h2>Universal Bulk Import</h2><p>One song per line: Title | Artist | Duration | direct audio URL | optional source URL</p><textarea id="bulk">${S.tracks.map(t=>`${t.title} | ${t.artist} | ${t.duration} | ${t.audioUrl||''} | ${t.sourceUrl||''}`).join('\n')}</textarea><button id="bulkBtn">Save & Publish Bulk Import</button></section>`)}
function announcePage(){return command(`<section class="panel"><h1>Instant Announcements</h1><textarea id="instant">${esc(S.instant)}</textarea><div class="actions"><button id="speak">Speak Now</button><button id="umbrellas" class="secondary">Close Umbrellas</button><button id="safety" class="secondary">Safety Hold</button></div><p class="status">Announcements are sent to the receiver, pause music, speak, then resume unless marked as a safety hold.</p></section><section class="panel"><h2>Birthday</h2><input id="birthdayName" placeholder="Guest name" value="${esc(S.birthdayName)}"><textarea id="birthdayText">${esc(S.birthdayText)}</textarea><button id="birthday">Speak Birthday</button></section><section class="panel"><h2>Owner / Manager Daily Messages</h2><textarea id="owner">${esc(S.owner)}</textarea><textarea id="manager">${esc(S.manager)}</textarea><button id="saveMsg">Save Daily Messages</button></section>`)}
function dailyPage(){let regs=S.regular.map((r,i)=>`<div class="q"><div><b>${esc(r.label||'Reminder')}</b><span>${esc(r.time)} · ${r.enabled?'enabled':'off'}</span><p>${esc(r.text)}</p></div><button data-reg="${i}">Delete</button></div>`).join('');return command(`<section class="panel"><h1>Regular Daily Announcements</h1><p>Use this for recurring reminders like closing warnings, safety reminders, and owner/manager notes.</p><label>Label<input id="regLabel" placeholder="Pool closing in 15 minutes"></label><label>Time<input id="regTime" type="time" value="20:45"></label><label>Message<textarea id="regText" placeholder="Message to play every day at this time"></textarea></label><button id="addReg">Add Daily Reminder</button></section><section class="panel"><h2>Active Daily Plan</h2>${regs||'<p>No regular reminders yet.</p>'}</section>`)}
function hoursPage(){return command(`<section class="panel"><h1>Pool Hours Automation</h1><div class="grid2"><label>Pool Opens<input id="poolOpen" type="time" value="${esc(S.poolOpen)}"></label><label>Pool Closes<input id="poolClose" type="time" value="${esc(S.poolClose)}"></label></div><label><select id="autoStart"><option value="true">Auto-start music during open hours</option><option value="false">Do not auto-start</option></select></label><label><select id="autoStop"><option value="true">Auto-stop music when closed</option><option value="false">Do not auto-stop</option></select></label><button id="saveHours">Save Pool Hours</button><p class="status">Browser rule: the sound-system browser must be opened and armed once. After that, admin commands and pool-hour automation can control it during the session.</p></section>`)}
function weatherPage(){return command(`<section class="panel"><h1>Location & Weather Safety</h1><label>Resort Address<input id="address" value="${esc(S.address)}"></label><div class="actions"><button id="verify">Verify Address</button><button id="device" class="secondary">Use Device GPS</button><button id="weatherCheck" class="secondary">Run Weather Check</button></div><div class="grid3"><label>Latitude<input id="lat" value="${esc(S.lat)}"></label><label>Longitude<input id="lon" value="${esc(S.lon)}"></label><label>Radius Miles<input id="radius" value="${esc(S.radius)}"></label></div><button id="saveLoc">Save Location</button><div class="status"><b>${esc(S.locationNote)}</b><p>${esc(S.weather)}</p></div></section>`)}
function voicePage(){let device=bestDeviceVoices().map(v=>`<option value="${esc(v.name)}" ${v.name===S.deviceVoice?'selected':''}>${esc(v.name)} · ${esc(v.lang)}</option>`).join('');return command(`<section class="panel"><h1>Voice Studio</h1><p>AI voice uses OpenAI gpt-4o-mini-tts with Marin/Cedar quality voices when billing/quota allows it. If OpenAI returns 429, the receiver falls back to free device voice instead of blocking.</p><label>Voice Mode<select id="voiceMode"><option value="ai">Natural AI Voice first</option><option value="device">Free Device Voice only</option></select></label><label>AI Voice<select id="aiVoice"><option value="marin">Marin - recommended</option><option value="cedar">Cedar - recommended</option><option value="coral">Coral</option><option value="nova">Nova</option><option value="sage">Sage</option><option value="shimmer">Shimmer</option><option value="onyx">Onyx</option></select></label><label>Best Device Voice<select id="deviceVoice"><option value="">Best available</option>${device}</select></label><div class="grid2"><label>Speed<input id="rate" type="range" min="0.75" max="1.15" step=".01" value="${S.voiceRate}"></label><label>Pitch<input id="pitch" type="range" min=".85" max="1.15" step=".01" value="${S.voicePitch}"></label></div><div class="actions"><button id="saveVoice">Save Voice</button><button id="testVoice" class="secondary">Test Voice</button></div></section>`)}
function render(){document.getElementById('app').innerHTML=S.screen==='home'?home():!S.admin?login():({station,announce:announcePage,daily:dailyPage,hours:hoursPage,weather:weatherPage,voice:voicePage}[S.tab]||station)();bind()}
function bind(){if($('homeBtn'))$('homeBtn').onclick=()=>{S.screen='home';local();render()};if($('cmdBtn'))$('cmdBtn').onclick=()=>{S.screen='command';local();render()};if($('login'))$('login').onclick=()=>{if(val('pin')===PIN){S.admin=true;S.screen='command';S.tab='station';local();render()}else alert('Wrong PIN')};document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{S.tab=b.dataset.tab;local();render()});if($('playHome'))$('playHome').onclick=()=>S.stationIntent==='playing'?pause(true):play(true);if($('skipHome'))$('skipHome').onclick=()=>advance(1,true);if($('stopHome'))$('stopHome').onclick=()=>stop(true);if($('platform'))$('platform').value=S.platform;if($('import'))$('import').onclick=importPlaylist;if($('playCmd'))$('playCmd').onclick=()=>play(true);if($('pauseCmd'))$('pauseCmd').onclick=()=>pause(true);if($('skipCmd'))$('skipCmd').onclick=()=>advance(1,true);if($('stopCmd'))$('stopCmd').onclick=()=>stop(true);document.querySelectorAll('[data-song]').forEach(b=>b.onclick=()=>{S.current=Number(b.dataset.song)||0;play(true)});if($('bulkBtn'))$('bulkBtn').onclick=bulkImport;if($('speak'))$('speak').onclick=()=>{S.instant=val('instant');sendAnnouncement(S.instant,false)};if($('umbrellas'))$('umbrellas').onclick=()=>sendAnnouncement('Attention guests, please close all umbrellas and secure loose items. Thank you.',false);if($('safety'))$('safety').onclick=()=>sendAnnouncement('Attention guests, please exit the pool area now and wait for further instructions from the lifeguards.',true);if($('birthday'))$('birthday').onclick=()=>{S.birthdayName=val('birthdayName');S.birthdayText=val('birthdayText');sendAnnouncement(`Happy birthday ${S.birthdayName}. ${S.birthdayText}`,false)};if($('saveMsg'))$('saveMsg').onclick=()=>{S.owner=val('owner');S.manager=val('manager');save(true)};if($('addReg'))$('addReg').onclick=()=>{let label=val('regLabel')||'Daily Reminder',time=val('regTime')||'12:00',text=val('regText');if(!text)return;S.regular.push({id:Date.now()+'',label,time,text,enabled:true});save(true)};document.querySelectorAll('[data-reg]').forEach(b=>b.onclick=()=>{S.regular.splice(Number(b.dataset.reg),1);save(true)});if($('poolOpen'))$('poolOpen').value=S.poolOpen;if($('poolClose'))$('poolClose').value=S.poolClose;if($('autoStart'))$('autoStart').value=String(!!S.autoStart);if($('autoStop'))$('autoStop').value=String(!!S.autoStop);if($('saveHours'))$('saveHours').onclick=()=>{S.poolOpen=val('poolOpen');S.poolClose=val('poolClose');S.autoStart=val('autoStart')==='true';S.autoStop=val('autoStop')==='true';save(true)};if($('verify'))$('verify').onclick=verify;if($('device'))$('device').onclick=deviceLoc;if($('weatherCheck'))$('weatherCheck').onclick=weatherCheck;if($('saveLoc'))$('saveLoc').onclick=()=>{S.address=val('address');S.lat=val('lat');S.lon=val('lon');S.radius=Number(val('radius'))||10;save(true)};if($('voiceMode'))$('voiceMode').value=S.voiceMode;if($('aiVoice'))$('aiVoice').value=S.aiVoice;if($('saveVoice'))$('saveVoice').onclick=()=>{S.voiceMode=val('voiceMode');S.aiVoice=val('aiVoice');S.deviceVoice=val('deviceVoice');S.voiceRate=Number(val('rate'))||.94;S.voicePitch=Number(val('pitch'))||1;save(true)};if($('testVoice'))$('testVoice').onclick=()=>speak('This is Serenity Shores Poolside Pulse version '+VERSION+'. This is a natural voice test for resort announcements.',{})}
function loadVoices(){voices=('speechSynthesis'in window)?speechSynthesis.getVoices():[];render()}
if('speechSynthesis'in window){speechSynthesis.onvoiceschanged=loadVoices;voices=speechSynthesis.getVoices()}render();pullState();setInterval(pullState,2000);setInterval(scheduleTick,30000);setInterval(weatherCheck,5*60*1000);
