const VERSION = '3.6';
const PIN = '7900';
const KEY = 'poolside-pulse-v36-local';
const OLD_KEY = 'poolside-pulse-v35-local';
const DEFAULT_PLAYLIST = 'https://suno.com/playlist/cf4b536e-9005-4c98-9ea5-a7f01eca116f';
const DEFAULT_ADDRESS = '615 Serenity Shores Ln, Kimberling City, MO 65686';
const DEFAULT_TRACKS = [
  { title: 'Load or import the Serenity Shores playlist', artist: 'Poolside Pulse', duration: '3:00', audioUrl: '', sourceUrl: '' }
];
const DEFAULT_CANNED = [
  { id: 'welcome', label: 'Welcome', text: 'Good morning and welcome to Serenity Shores. We are glad your family is here. Please supervise children at all times, keep glass out of the pool area, and follow lifeguard instructions so everyone can enjoy a safe, easy day by the water.' },
  { id: 'standard', label: 'General Notice', text: 'Attention guests, this is a Serenity Shores pool announcement.' },
  { id: 'manager', label: 'Manager Message', text: 'This is a friendly Serenity Shores reminder: safety comes first, children must be supervised, and lifeguard instructions should be followed right away.' },
  { id: 'owner', label: 'Owner Message', text: 'From all of us at Serenity Shores, thank you for spending part of your vacation with us. We hope this place feels peaceful, fun, and memorable for your family.' },
  { id: 'birthday', label: 'Birthday', text: 'Happy birthday, {name}, from Serenity Shores. We hope your day is absolutely wonderful.' },
  { id: 'umbrellas', label: 'Close Umbrellas', text: 'Attention guests, please close all umbrellas and secure loose items. Thank you.' },
  { id: 'poolclose15', label: 'Closing 15', text: 'Attention guests, the pool will close in 15 minutes. Please begin gathering your belongings and make sure children have everything they brought with them.' },
  { id: 'poolclose5', label: 'Closing 5', text: 'Attention guests, the pool will close in 5 minutes. Thank you for spending the day at Serenity Shores. Please exit the pool calmly when lifeguards give the final instruction.' },
  { id: 'safetybreak', label: 'Safety Break', text: 'Attention guests, we are taking a short safety break. Please clear the pool and follow lifeguard instructions.' },
  { id: 'hydration', label: 'Hydration', text: 'Friendly reminder from Serenity Shores: take a water break, reapply sunscreen, and keep an eye on younger swimmers.' },
  { id: 'noglass', label: 'No Glass', text: 'Friendly reminder: glass is not permitted in the pool area. Thank you for helping us keep the pool safe for everyone.' },
  { id: 'weatherwatch', label: 'Weather Watch', text: 'Attention guests, weather is being monitored near Serenity Shores. Please stay alert for instructions from lifeguards.' },
  { id: 'lostchild', label: 'Lost Child', text: 'Attention guests, we need everyone to pause and help reunite a child with their family. Please listen for staff instructions.' }
];
const DEFAULT_SCHEDULE = [
  { id: 'sched-open', label: 'Pool Open Welcome', type: 'announcement', time: '09:05', announcementId: 'welcome', enabled: true },
  { id: 'sched-10-welcome', label: '10am Welcome', type: 'announcement', time: '10:00', announcementId: 'welcome', enabled: true },
  { id: 'sched-noon-rules', label: 'Lunch Safety Reminder', type: 'announcement', time: '12:30', announcementId: 'noglass', enabled: true },
  { id: 'sched-afternoon-hydrate', label: 'Afternoon Hydration', type: 'announcement', time: '15:00', announcementId: 'hydration', enabled: true },
  { id: 'sched-evening-manager', label: 'Evening Manager Reminder', type: 'announcement', time: '18:00', announcementId: 'manager', enabled: false },
  { id: 'close15', label: 'Closing 15', type: 'announcement', offsetToClose: 15, announcementId: 'poolclose15', enabled: true },
  { id: 'close5', label: 'Closing 5', type: 'announcement', offsetToClose: 5, announcementId: 'poolclose5', enabled: true }
];
const BASE = { admin:false, screen:'home', tab:'station', sync:false, syncMode:'starting', syncNote:'Connecting station control...', revision:0, updatedAt:0, platform:'suno', playlistName:'Serenity Shores Poolside Pulse', playlistUrl:DEFAULT_PLAYLIST, tracks:DEFAULT_TRACKS, current:0, stationIntent:'stopped', command:null, announcement:null, lastError:'', selectedCannedId:'welcome', canned:DEFAULT_CANNED, voiceMode:'ai', aiVoice:'marin', deviceVoice:'', voiceRate:0.94, voicePitch:1, poolOpen:'09:00', poolClose:'22:00', autoStart:true, autoStop:true, address:DEFAULT_ADDRESS, lat:'', lon:'', radius:10, locationNote:'Serenity Shores address is ready to verify. Device GPS at the resort is best.', weather:'Weather monitor is standing by.', weatherCheckedAt:'', birthdayName:'', scheduleVersion:36, lastRun:{}, regular:DEFAULT_SCHEDULE, log:[] };
let S = load();
let voices = [];
let music = new Audio();
let announcing = false;
let lastCommandId = '';
let lastAnnouncementId = '';
let armReady = false;
let importing = false;
let fading = null;
music.preload = 'auto';
music.volume = 0.95;
music.onended = () => advance(1, false);
music.onerror = () => { S.lastError = 'Audio failed for this track. Skipping.'; advance(1, true); };
function clone(o){return JSON.parse(JSON.stringify(o));}
function esc(x){return String(x ?? '').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));}
function $(id){return document.getElementById(id);}
function val(id){return $(id)?.value || '';}
function now(){return new Date().toLocaleTimeString([], {hour:'numeric', minute:'2-digit'});}
function nowFull(){return new Date().toLocaleString([], {month:'short', day:'numeric', hour:'numeric', minute:'2-digit'});}
function log(msg){S.log=[[msg,now()],...(S.log||[])].slice(0,80);local();}
function track(){return S.tracks[S.current] || DEFAULT_TRACKS[0];}
function audioTracks(){return (S.tracks||[]).filter(t=>t.audioUrl);}
function minutes(hhmm){const [h,m]=String(hhmm||'0:0').split(':').map(Number);return (h||0)*60+(m||0);}
function hhmm(total){total=((Number(total)||0)%1440+1440)%1440;return String(Math.floor(total/60)).padStart(2,'0')+':'+String(total%60).padStart(2,'0');}
function displayTime(h){const [a,b]=String(h||'00:00').split(':').map(Number);const s=a>=12?'PM':'AM';return `${((a+11)%12)+1}:${String(b||0).padStart(2,'0')} ${s}`;}
function scheduleTime(r){return Number.isFinite(Number(r.offsetToClose)) ? hhmm(minutes(S.poolClose)-Number(r.offsetToClose)) : (r.time||'12:00');}
function scheduleSort(a,b){return minutes(scheduleTime(a))-minutes(scheduleTime(b));}
function dayKey(){const d=new Date();return `${d.getFullYear()}-${d.getMonth()+1}-${d.getDate()}`;}
function runKey(r){return `${dayKey()}:${r.id}:${scheduleTime(r)}`;}
function inPoolHours(){const n=new Date();const m=n.getHours()*60+n.getMinutes();const a=minutes(S.poolOpen), b=minutes(S.poolClose);return a<=b ? m>=a&&m<b : m>=a||m<b;}
function weatherStamp(){return S.weatherCheckedAt ? `Last checked ${S.weatherCheckedAt}` : 'Not checked yet';}
function syncText(){return S.sync ? `Sync Active · ${S.syncMode}` : 'Preview sync';}
function updateSyncPill(){const el=$('syncPill'); if(el) el.textContent=`V${VERSION} · ${syncText()}`;}
function showStatus(message){S.lastError=message;local();render();}
function mergeCanned(existing, raw){ const map=new Map(); DEFAULT_CANNED.forEach(item=>map.set(item.id,clone(item))); (existing||[]).forEach(item=>{const id=item.id||String(Date.now()+Math.random()); map.set(id,{...clone(item),id});}); const out=[...map.values()]; if(raw?.owner){const o=out.find(x=>x.id==='owner'); if(o&&o.text===DEFAULT_CANNED.find(x=>x.id==='owner').text) o.text=raw.owner;} if(raw?.manager){const m=out.find(x=>x.id==='manager'); if(m&&m.text===DEFAULT_CANNED.find(x=>x.id==='manager').text) m.text=raw.manager;} return out; }
function normalizeSchedule(existing){ const byId=new Map(); (existing||[]).forEach(r=>{const id=r.id||String(Date.now()+Math.random()); byId.set(id,{...r,id});}); DEFAULT_SCHEDULE.forEach(def=>{ if(byId.has(def.id)){const old=byId.get(def.id); byId.set(def.id,{...def,...old,announcementId:old.announcementId||def.announcementId,offsetToClose:def.offsetToClose??old.offsetToClose,enabled:old.enabled??def.enabled});} else byId.set(def.id,clone(def)); }); return [...byId.values()].map(r=>{ if(r.id==='close15') return {...r,offsetToClose:15,announcementId:r.announcementId||'poolclose15'}; if(r.id==='close5') return {...r,offsetToClose:5,announcementId:r.announcementId||'poolclose5'}; return r; }).sort(scheduleSort); }
function clean(raw){ const x=raw||{}; const s={...clone(BASE),...x}; if(s.tab==='announce'||s.tab==='daily') s.tab='schedule'; s.tracks=Array.isArray(s.tracks)&&s.tracks.length?s.tracks:clone(DEFAULT_TRACKS); s.canned=mergeCanned(s.canned,x); s.regular=normalizeSchedule(s.regular); s.log=Array.isArray(s.log)?s.log:[]; s.lastRun=s.lastRun&&typeof s.lastRun==='object'?s.lastRun:{}; s.current=Math.min(Math.max(Number(s.current)||0,0),Math.max(s.tracks.length-1,0)); s.scheduleVersion=36; return s; }
function load(){for(const k of [KEY,OLD_KEY]){try{const raw=localStorage.getItem(k);if(raw)return clean(JSON.parse(raw));}catch{}}return clean({});}
function local(){localStorage.setItem(KEY,JSON.stringify(S));}
function publicState(){const keep=clone(S);keep.admin=false;keep.screen='home';keep.tab='station';return keep;}
async function save(push=true){local(); if(push) await pushState(); render();}
async function pushState(){ try{const r=await fetch('/api/state',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({state:publicState()})});const d=await r.json();S.sync=!!d.cloudSync;S.syncMode=d.syncMode||'server';S.syncNote=d.note||'Shared station control active.';if(d.state){S.revision=d.state.revision||S.revision;S.updatedAt=d.state.savedAt||S.updatedAt;}local();return d;} catch(e){S.sync=false;S.syncMode='local';S.syncNote='Shared control unavailable: '+e.message;local();return null;} }
async function pullState(){ try{const r=await fetch('/api/state',{cache:'no-store'});const d=await r.json();S.sync=!!d.cloudSync;S.syncMode=d.syncMode||'server';S.syncNote=d.note||'Shared station control active.';if(d.state&&Number(d.state.revision||0)>Number(S.revision||0)){const keep={admin:S.admin,screen:S.screen,tab:S.tab,lastError:S.lastError,selectedCannedId:S.selectedCannedId};S=clean({...S,...d.state,...keep});local();await applyRemote();render();}else{local();updateSyncPill();}} catch{S.sync=false;S.syncMode='local';S.syncNote='Shared control temporarily unavailable.';updateSyncPill();} }
async function fetchJson(url,opts){const r=await fetch(url,opts);const d=await r.json().catch(()=>({}));if(!r.ok||d.ok===false)throw Error(d.error||`HTTP ${r.status}`);return d;}
async function importFromSunoUrl(url,replaceQueue=true){ if(!url)throw Error('Missing Suno URL.'); const d=await fetchJson('/api/suno-playlist?url='+encodeURIComponent(url)); const tracks=(d.tracks||[]).map(t=>({title:t.title||'Untitled',artist:t.artist||'Suno',duration:t.duration||'3:00',audioUrl:t.audioUrl||'',sourceUrl:t.sourceUrl||url,imageUrl:t.imageUrl||''})); if(!tracks.length)throw Error(d.error||'No tracks found.'); if(replaceQueue){S.tracks=tracks;if(d.playlistName)S.playlistName=d.playlistName;S.playlistUrl=url;S.current=0;} S.lastError=d.audioWarning||d.warning||'';log(`Imported ${tracks.length} Suno track${tracks.length===1?'':'s'}`);return tracks; }
async function ensurePlaylistLoaded(){ if(importing)return false; if(audioTracks().length)return true; if(!S.playlistUrl)return false; importing=true;S.lastError='Importing playlist audio...';render(); try{await importFromSunoUrl(S.playlistUrl,true);importing=false;await save(true);return audioTracks().length>0;} catch(e){S.lastError='Playlist import failed: '+e.message;importing=false;local();render();return false;} }
function setMusicSource(t,keepIfSame=true){if(!t?.audioUrl)return false;if(!keepIfSame||music.src!==t.audioUrl)music.src=t.audioUrl;return true;}
async function fadeTo(target=1,ms=600){ if(fading)clearInterval(fading);const start=Number.isFinite(music.volume)?music.volume:0.95;const steps=Math.max(8,Math.round(ms/40));let i=0; return new Promise(resolve=>{fading=setInterval(()=>{i++;music.volume=Math.max(0,Math.min(1,start+(target-start)*(i/steps)));if(i>=steps){clearInterval(fading);fading=null;music.volume=target;resolve();}},ms/steps);}); }
async function startCurrentTrack(push=true,reset=false){ armReady=true;const ok=await ensurePlaylistLoaded();if(!ok){S.stationIntent='stopped';S.lastError='No playable audio is loaded yet. Import the Suno playlist in Command Center.';await save(push);return;} let t=track();if(!t.audioUrl){const idx=S.tracks.findIndex(x=>x.audioUrl);if(idx>=0){S.current=idx;t=track();}} if(!setMusicSource(t,!reset))return;if(reset)music.currentTime=0; try{music.volume=Math.max(music.volume||0.01,0.01);await music.play();await fadeTo(0.95,700);S.stationIntent='playing';S.lastError='';log('Playing '+t.title);}catch{S.stationIntent='stopped';S.lastError='This browser blocked playback. Tap Arm / Play Station once on the sound-system device.';log('Playback blocked');} if(push){S.command={id:Date.now()+':play',type:'play'};await save(true);}else{local();render();} }
async function play(push=true){return startCurrentTrack(push,false);}
async function pause(push=true){await fadeTo(0,350);music.pause();music.volume=0.95;S.stationIntent='paused';if(push){S.command={id:Date.now()+':pause',type:'pause'};await save(true);}else{local();render();}}
async function stop(push=true){await fadeTo(0,450);music.pause();music.currentTime=0;music.volume=0.95;S.stationIntent='stopped';if(push){S.command={id:Date.now()+':stop',type:'stop'};await save(true);}else{local();render();}}
async function advance(n=1,push=true){S.current=(S.current+n+S.tracks.length)%S.tracks.length;if(S.stationIntent==='playing'){if(push)S.command={id:Date.now()+':skip',type:'skip',n};await save(push);await startCurrentTrack(false,true);}else if(push){S.command={id:Date.now()+':skip',type:'skip',n};await save(true);}else{local();render();}}
async function playSunoUrlAtReceiver(url,label='Scheduled Suno'){try{await importFromSunoUrl(url,true);S.playlistName=label||S.playlistName;S.current=0;await save(false);await startCurrentTrack(false,true);}catch(e){S.lastError='Scheduled Suno failed: '+e.message;local();render();}}
function selectedCanned(){return S.canned.find(x=>x.id===S.selectedCannedId)||S.canned[0]||DEFAULT_CANNED[0];}
function announcementTextById(id){const c=S.canned.find(x=>x.id===id);return c?c.text:'';}
function renderTokens(text){return String(text||'').replaceAll('{name}',S.birthdayName||'friend').replaceAll('{resort}','Serenity Shores').replaceAll('{open}',displayTime(S.poolOpen)).replaceAll('{close}',displayTime(S.poolClose));}
async function prepAnnouncement(){announcing=true;const resume=S.stationIntent==='playing'&&!music.paused;const position=music.currentTime||0;const src=music.src;if(resume){await fadeTo(0,700);music.pause();}return{resume,position,src};}
async function finishAnnouncement(info,hold){announcing=false;if(info.resume&&!hold&&inPoolHours()){try{if(info.src&&music.src!==info.src)music.src=info.src;if(Number.isFinite(info.position))music.currentTime=info.position;music.volume=0.01;await music.play();await fadeTo(0.95,1200);S.stationIntent='playing';}catch{S.lastError='Music could not resume automatically. Tap Play on receiver.';}}local();render();}
async function speak(text,opts={}){ const msg=renderTokens(text).trim();if(!msg)return;const info=await prepAnnouncement();const hold=!!opts.hold;let usedAi=false; if(S.voiceMode==='ai'){ try{const r=await fetch('/api/tts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text:msg,voice:S.aiVoice||'marin',instructions:'Speak like a calm, polished, realistic resort public-address announcer at Serenity Shores. Use clear diction. For safety, sound firm without panic.'})}); if(r.ok){const blob=await r.blob();const url=URL.createObjectURL(blob);const a=new Audio(url);a.volume=1;a.onended=()=>{URL.revokeObjectURL(url);finishAnnouncement(info,hold);};await a.play();usedAi=true;log('AI voice announcement played');return;} const d=await r.json().catch(()=>({}));S.lastError=`AI voice unavailable (${r.status}). Device voice fallback used.`+(d.detail?' Check billing/limits.':'');local();} catch{S.lastError='AI voice failed. Device voice fallback used.';local();} } if(!usedAi){if(!('speechSynthesis'in window)){await finishAnnouncement(info,hold);return;}speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(msg);const v=voices.find(x=>x.name===S.deviceVoice)||bestDeviceVoices()[0];if(v)u.voice=v;u.rate=Number(S.voiceRate)||0.94;u.pitch=Number(S.voicePitch)||1;u.onend=()=>finishAnnouncement(info,hold);speechSynthesis.speak(u);log('Device voice announcement played');} }
async function sendAnnouncement(text,hold=false){const msg=renderTokens(text).trim();if(!msg)return;S.announcement={id:Date.now()+':'+Math.random().toString(36).slice(2),text:msg,hold,createdAt:Date.now()};log('Announcement sent');await save(true);if(S.screen==='home')speak(msg,{hold});}
async function applyRemote(){if(S.screen!=='home')return;const cmd=S.command;if(cmd?.id&&cmd.id!==lastCommandId&&!announcing){lastCommandId=cmd.id;if(cmd.type==='play')await play(false);if(cmd.type==='pause')await pause(false);if(cmd.type==='stop')await stop(false);if(cmd.type==='skip')await advance(cmd.n||1,false);if(cmd.type==='suno')await playSunoUrlAtReceiver(cmd.url,cmd.label);}const a=S.announcement;if(a?.id&&a.id!==lastAnnouncementId){lastAnnouncementId=a.id;await speak(a.text,{hold:a.hold});}}
function bestDeviceVoices(){return voices.filter(v=>/^en/i.test(v.lang||'')).sort((a,b)=>voiceScore(b)-voiceScore(a)).slice(0,20);}
function voiceScore(v){const n=(v.name+' '+v.lang).toLowerCase();let s=0;['samantha','ava','allison','siri','premium','enhanced','natural','google','microsoft','aria','jenny','serena'].forEach((p,i)=>{if(n.includes(p))s+=50-i;});if(n.includes('en-us'))s+=10;return s;}
async function importPlaylist(){S.platform=val('platform');S.playlistName=val('playlistName')||S.playlistName;S.playlistUrl=val('playlistUrl')||S.playlistUrl;if(S.platform!=='suno'){S.lastError='Saved as a reference. Automatic playable import currently works for Suno. Use Bulk Import for direct audio URLs from other platforms.';await save(true);return;}await ensurePlaylistLoaded();await save(true);}
async function bulkImport(){const rows=val('bulk').split('\n').map(x=>x.trim()).filter(Boolean);if(!rows.length)return;S.tracks=rows.map(r=>{const p=r.split('|').map(x=>x.trim());return{title:p[0]||'Untitled',artist:p[1]||'Unknown',duration:p[2]||'3:00',audioUrl:p[3]||'',sourceUrl:p[4]||''};});S.current=0;S.lastError='Bulk import saved and published.';await save(true);}
async function verify(){S.address=val('address')||S.address;try{const d=await fetchJson('/api/geocode?address='+encodeURIComponent(S.address));S.lat=String(d.latitude);S.lon=String(d.longitude);S.locationNote=(d.fallback?'Best available match. ':'Verified: ')+(d.matchedAddress||S.address);S.weather='Location ready. Weather monitoring can run.';S.weatherCheckedAt='';await save(true);}catch(e){S.locationNote='Address lookup failed. Use Device GPS while physically at Serenity Shores, or enter coordinates manually.';S.lastError=e.message;await save(true);}}
function deviceLoc(){navigator.geolocation?.getCurrentPosition(async p=>{S.lat=p.coords.latitude.toFixed(6);S.lon=p.coords.longitude.toFixed(6);S.locationNote='Device GPS captured with about '+Math.round(p.coords.accuracy||0)+'m accuracy.';await save(true);},async()=>{S.locationNote='Device GPS permission failed.';await save(true);},{enableHighAccuracy:true,timeout:12000});}
async function weatherCheck(){if(!S.lat||!S.lon){S.weather='Verify location first.';S.weatherCheckedAt=nowFull();render();return;}try{const d=await fetchJson(`/api/weather?lat=${S.lat}&lon=${S.lon}&radiusMiles=${Number(S.radius)||10}`);S.weather=d.summary||'Weather checked.';S.weatherCheckedAt=nowFull();if(d.threat){await sendAnnouncement('Attention guests. Weather activity has been detected nearby. Please exit the pool area and move indoors immediately.',true);S.stationIntent='paused';}await save(true);}catch(e){S.weather='Weather check failed: '+e.message;S.weatherCheckedAt=nowFull();await save(true);}}
async function scheduleTick(){const within=inPoolHours();if(S.screen==='home'&&armReady&&!announcing){if(S.autoStart&&within&&S.stationIntent!=='playing')await play(false);if(S.autoStop&&!within&&S.stationIntent==='playing')await stop(false);const n=new Date();const hm=String(n.getHours()).padStart(2,'0')+':'+String(n.getMinutes()).padStart(2,'0');for(const r of S.regular||[]){if(!r.enabled||scheduleTime(r)!==hm)continue;const key=runKey(r);if(S.lastRun?.[r.id]===key)continue;S.lastRun={...(S.lastRun||{}),[r.id]:key};if((r.type||'announcement')==='suno'){S.command={id:Date.now()+':suno',type:'suno',url:r.url,label:r.label};await save(true);await playSunoUrlAtReceiver(r.url,r.label);}else{const text=r.announcementId?announcementTextById(r.announcementId):r.text;await save(true);await sendAnnouncement(text,!!r.hold);}}}}
function head(){return `<header class="top"><div class="brand"><b>SS</b><span>Serenity Shores<small>Poolside Pulse · V${VERSION}</small></span></div><div class="tog"><button id="homeBtn" class="${S.screen==='home'?'active':''}">Home</button><button id="cmdBtn" class="${S.screen!=='home'?'active':''}">Command</button></div></header>`;}
function home(){ const t=track(); const armed=armReady?'Receiver armed':'Tap once to arm receiver'; const nextItems=(S.regular||[]).filter(r=>r.enabled).sort(scheduleSort).slice(0,4).map(r=>`<p class="line"><b>${displayTime(scheduleTime(r))}</b><span>${esc(r.label)}</span></p>`).join(''); return `${head()}<main class="home"><section class="hero compact"><div><p class="tag">PRIVATE RESORT STATION · V${VERSION}</p><h1>Poolside Pulse</h1><p class="sub">Command-controlled resort music, announcements, birthdays, daily messages, and weather-aware pool safety.</p><div class="actions"><button id="playHome">${S.stationIntent==='playing'?'Pause':'Arm / Play Station'}</button><button id="skipHome" class="secondary">Skip</button><button id="stopHome" class="secondary">Stop</button></div><p class="mini">${armed} · ${syncText()}</p></div><aside><b>Pool Hours</b><p>${esc(S.poolOpen)} – ${esc(S.poolClose)}</p><b>Weather</b><p>${esc(S.weather)}</p><small>${esc(weatherStamp())}</small></aside></section><section class="now"><div class="art">SS<span>Pulse</span></div><div><p class="tag teal">${S.stationIntent==='playing'?'NOW PLAYING':'STATION READY'}</p><h2>${esc(t.title)}</h2><p>${esc(t.artist)} · ${esc(t.duration)}</p><div class="bar"><i></i></div><p>${esc(S.playlistName)}</p>${S.lastError?`<p class="warn">${esc(S.lastError)}</p>`:''}</div></section><section class="cards"><div class="card"><h3>Next Scheduled</h3>${nextItems||'<p>No enabled daily schedule items.</p>'}</div><div class="card"><h3>Weather Monitor</h3><p>${esc(S.weather)}</p><p><b>${esc(weatherStamp())}</b></p></div></section></main>`; }
function login(){return `${head()}<main class="wrap"><section class="card login"><h1>Command Center</h1><p>PIN protected station controls.</p><input id="pin" inputmode="numeric" type="password" placeholder="PIN"><button id="login">Enter</button><p class="mini dark">V${VERSION}</p></section></main>`;}
function tabs(){const a=[['station','Music'],['schedule','Announcement Schedule'],['hours','Hours'],['weather','Weather'],['voice','Voice']];return `<nav class="tabs stickyTabs">${a.map(x=>`<button data-tab="${x[0]}" class="${S.tab===x[0]?'on':''}">${x[1]}</button>`).join('')}</nav>`;}
function command(body){return `${head()}<main class="wrap"><div class="version" id="syncPill">V${VERSION} · ${syncText()}</div>${tabs()}${S.lastError?`<div class="status"><b>Status:</b> ${esc(S.lastError)}</div>`:''}${body}</main>`;}
function station(){ const q=S.tracks.map((t,i)=>`<div class="q ${i===S.current?'cur':''}"><div><b>${i+1}. ${esc(t.title)}</b><span>${esc(t.artist)} · ${esc(t.duration)} ${t.audioUrl?'· playable':'· title only'}</span></div><button data-song="${i}">Play</button></div>`).join(''); return command(`<section class="panel tight"><h1>Music Source</h1><p>The last published playlist is what every armed Home receiver plays when no announcement is running.</p><label>Platform<select id="platform"><option value="suno">Suno automatic playable import</option><option value="youtube">YouTube reference/manual</option><option value="spotify">Spotify reference/manual</option><option value="amazon">Amazon reference/manual</option><option value="google">Google/YouTube Music reference/manual</option></select></label><label>Playlist or Song URL<input id="playlistUrl" value="${esc(S.playlistUrl)}"></label><label>Playlist Name<input id="playlistName" value="${esc(S.playlistName)}"></label><div class="actions"><button id="import">Import & Publish</button><button id="playCmd" class="secondary">Play Receivers</button><button id="pauseCmd" class="secondary">Pause</button><button id="skipCmd" class="secondary">Skip</button><button id="stopCmd" class="secondary">Stop</button></div></section><section class="panel tight"><h2>Published Queue</h2>${q}</section><section class="panel tight"><h2>Universal Bulk Import</h2><p>One song per line: Title | Artist | Duration | direct audio URL | optional source URL</p><textarea id="bulk">${S.tracks.map(t=>`${t.title} | ${t.artist} | ${t.duration} | ${t.audioUrl||''} | ${t.sourceUrl||''}`).join('\n')}</textarea><button id="bulkBtn">Save & Publish Bulk Import</button></section>`); }
function schedulePage(){ const selected=selectedCanned(); const chips=S.canned.map(c=>`<button class="chip ${c.id===selected.id?'on':''}" data-canned="${esc(c.id)}">${esc(c.label)}</button>`).join(''); const options=S.canned.map(c=>`<option value="${esc(c.id)}" ${c.id===selected.id?'selected':''}>${esc(c.label)}</option>`).join(''); const rows=(S.regular||[]).slice().sort(scheduleSort).map((r,i)=>{const text=r.type==='suno'?(r.url||''):(r.announcementId?announcementTextById(r.announcementId):r.text||'');const dynamic=Number.isFinite(Number(r.offsetToClose))?`${r.offsetToClose} minutes before close`:'fixed time';return `<div class="q scheduleRow ${r.enabled?'':'off'}"><div><b>${displayTime(scheduleTime(r))} · ${esc(r.label||'Scheduled Item')}</b><span>${r.enabled?'enabled':'off'} · ${esc(r.type||'announcement')} · ${dynamic}</span><p>${esc(renderTokens(text))}</p></div><div class="rowBtns"><button data-toggle-reg="${i}" class="secondary">${r.enabled?'Turn Off':'Turn On'}</button><button data-reg="${i}" class="secondary">Delete</button></div></div>`;}).join(''); return command(`<section class="panel tight"><h1>Announcement Schedule</h1><p>Saved announcements can be played instantly or placed into the daily schedule. Music runs between announcements, then fades back in.</p><div class="chipgrid">${chips}<button id="newCanned" class="chip add">+ Add</button></div><div class="grid2"><label>Button Name<input id="cannedLabel" value="${esc(selected.label)}"></label><label>Guest Name Token<input id="birthdayName" placeholder="Optional, for birthday {name}" value="${esc(S.birthdayName)}"></label></div><label>Announcement Text<textarea id="cannedText">${esc(selected.text)}</textarea></label><div class="actions"><button id="playCanned">Play Now</button><button id="saveCanned" class="secondary">Save Button</button><button id="holdCanned" class="secondary">Play as Safety Hold</button><button id="deleteCanned" class="secondary">Delete</button></div></section><section class="panel tight"><h2>Add Announcement to Daily Schedule</h2><div class="grid2"><label>Announcement<select id="schedAnnouncement">${options}</select></label><label>Schedule Label<input id="schedLabel" placeholder="Optional custom label"></label></div><div class="grid2"><label>When<select id="schedRule"><option value="fixed">Specific time</option><option value="close30">30 minutes before close</option><option value="close15">15 minutes before close</option><option value="close5">5 minutes before close</option></select></label><label>Specific Time<input id="schedTime" type="time" value="10:00"></label></div><button id="addSched">Add Announcement to Daily Schedule</button></section><section class="panel tight"><h2>Schedule a Suno URL</h2><p>Use this when you want one Suno song or playlist to take over at a specific time.</p><div class="grid2"><label>Name<input id="sunoLabel" placeholder="Afternoon Feature"></label><label>Time<input id="sunoTime" type="time" value="14:00"></label></div><label>Suno Song or Playlist URL<input id="sunoUrl" placeholder="https://suno.com/song/... or https://suno.com/playlist/..."></label><button id="addSunoSched">Add Suno URL to Schedule</button></section><section class="panel tight"><h2>Daily Schedule</h2><p>Closing announcements are tied to pool close time, so changing pool hours keeps Closing 15 and Closing 5 correct.</p>${rows||'<p>No scheduled items yet.</p>'}</section>`); }
function hoursPage(){return command(`<section class="panel tight"><h1>Pool Hours Automation</h1><div class="grid2"><label>Pool Opens<input id="poolOpen" type="time" value="${esc(S.poolOpen)}"></label><label>Pool Closes<input id="poolClose" type="time" value="${esc(S.poolClose)}"></label></div><label>Auto-start<select id="autoStart"><option value="true">Auto-start music during open hours</option><option value="false">Do not auto-start</option></select></label><label>Auto-stop<select id="autoStop"><option value="true">Auto-stop music when closed</option><option value="false">Do not auto-stop</option></select></label><button id="saveHours">Save Pool Hours</button><p class="status">Receiver rule: the sound-system browser must be opened and armed once. After that, admin commands and pool-hour automation control it during that session.</p></section>`);}
function weatherPage(){return command(`<section class="panel tight"><h1>Location & Weather Safety</h1><label>Resort Address<input id="address" value="${esc(S.address)}"></label><div class="actions"><button id="verify">Verify Address</button><button id="device" class="secondary">Use Device GPS</button><button id="weatherCheck" class="secondary">Run Weather Check</button></div><div class="grid3"><label>Latitude<input id="lat" value="${esc(S.lat)}"></label><label>Longitude<input id="lon" value="${esc(S.lon)}"></label><label>Radius Miles<input id="radius" value="${esc(S.radius)}"></label></div><button id="saveLoc">Save Location</button><div class="status"><b>${esc(S.locationNote)}</b><p>${esc(S.weather)}</p><p><b>${esc(weatherStamp())}</b></p></div></section>`);}
function voicePage(){const device=bestDeviceVoices().map(v=>`<option value="${esc(v.name)}" ${v.name===S.deviceVoice?'selected':''}>${esc(v.name)} · ${esc(v.lang)}</option>`).join('');return command(`<section class="panel tight"><h1>Voice Studio</h1><p>AI voice uses OpenAI gpt-4o-mini-tts with Marin/Cedar quality voices when billing/quota allows it. 429 means billing, quota, project limit, or rate limit.</p><label>Voice Mode<select id="voiceMode"><option value="ai">Natural AI Voice first</option><option value="device">Free Device Voice only</option></select></label><label>AI Voice<select id="aiVoice"><option value="marin">Marin - recommended</option><option value="cedar">Cedar - recommended</option><option value="coral">Coral</option><option value="nova">Nova</option><option value="sage">Sage</option><option value="shimmer">Shimmer</option><option value="onyx">Onyx</option></select></label><label>Best Device Voice<select id="deviceVoice"><option value="">Best available</option>${device}</select></label><div class="grid2"><label>Speed<input id="rate" type="range" min="0.75" max="1.15" step=".01" value="${S.voiceRate}"></label><label>Pitch<input id="pitch" type="range" min=".85" max="1.15" step=".01" value="${S.voicePitch}"></label></div><div class="actions"><button id="saveVoice">Save Voice</button><button id="testVoice" class="secondary">Test Voice</button></div></section>`);}
function render(){const pages={station,schedule:schedulePage,hours:hoursPage,weather:weatherPage,voice:voicePage};document.getElementById('app').innerHTML=S.screen==='home'?home():!S.admin?login():(pages[S.tab]||station)();bind();}
function bind(){ if($('homeBtn'))$('homeBtn').onclick=()=>{S.screen='home';local();render();}; if($('cmdBtn'))$('cmdBtn').onclick=()=>{S.screen='command';local();render();}; if($('login'))$('login').onclick=()=>{if(val('pin')===PIN){S.admin=true;S.screen='command';S.tab='station';local();render();}else alert('Wrong PIN');}; document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{S.tab=b.dataset.tab;local();render();}); if($('playHome'))$('playHome').onclick=()=>S.stationIntent==='playing'?pause(true):play(true); if($('skipHome'))$('skipHome').onclick=()=>advance(1,true); if($('stopHome'))$('stopHome').onclick=()=>stop(true); if($('platform'))$('platform').value=S.platform; if($('import'))$('import').onclick=importPlaylist; if($('playCmd'))$('playCmd').onclick=()=>play(true); if($('pauseCmd'))$('pauseCmd').onclick=()=>pause(true); if($('skipCmd'))$('skipCmd').onclick=()=>advance(1,true); if($('stopCmd'))$('stopCmd').onclick=()=>stop(true); document.querySelectorAll('[data-song]').forEach(b=>b.onclick=()=>{S.current=Number(b.dataset.song)||0;startCurrentTrack(true,true);}); if($('bulkBtn'))$('bulkBtn').onclick=bulkImport; document.querySelectorAll('[data-canned]').forEach(b=>b.onclick=()=>{S.selectedCannedId=b.dataset.canned;local();render();}); if($('newCanned'))$('newCanned').onclick=()=>{const id='custom-'+Date.now();S.canned.push({id,label:'New Button',text:'Type announcement text here.'});S.selectedCannedId=id;save(true);}; if($('saveCanned'))$('saveCanned').onclick=()=>{const c=selectedCanned();c.label=val('cannedLabel')||c.label;c.text=val('cannedText')||c.text;S.birthdayName=val('birthdayName')||S.birthdayName;save(true);}; if($('deleteCanned'))$('deleteCanned').onclick=()=>{if(S.canned.length<=1)return;S.canned=S.canned.filter(c=>c.id!==S.selectedCannedId);S.regular=S.regular.filter(r=>r.announcementId!==S.selectedCannedId);S.selectedCannedId=S.canned[0]?.id||'welcome';save(true);}; if($('playCanned'))$('playCanned').onclick=()=>{const c=selectedCanned();c.label=val('cannedLabel')||c.label;c.text=val('cannedText')||c.text;S.birthdayName=val('birthdayName')||S.birthdayName;sendAnnouncement(c.text,false);}; if($('holdCanned'))$('holdCanned').onclick=()=>{const c=selectedCanned();c.label=val('cannedLabel')||c.label;c.text=val('cannedText')||c.text;S.birthdayName=val('birthdayName')||S.birthdayName;sendAnnouncement(c.text,true);}; if($('schedAnnouncement'))$('schedAnnouncement').onchange=e=>{S.selectedCannedId=e.target.value;local();}; if($('addSched'))$('addSched').onclick=()=>{const announcementId=val('schedAnnouncement')||S.selectedCannedId;const chosen=S.canned.find(c=>c.id===announcementId)||selectedCanned();const rule=val('schedRule');const item={id:'sched-'+Date.now(),label:val('schedLabel')||chosen.label,type:'announcement',announcementId,enabled:true};if(rule==='close15')item.offsetToClose=15;else if(rule==='close5')item.offsetToClose=5;else if(rule==='close30')item.offsetToClose=30;else item.time=val('schedTime')||'10:00';S.regular.push(item);S.regular=S.regular.sort(scheduleSort);save(true);}; if($('addSunoSched'))$('addSunoSched').onclick=()=>{const url=val('sunoUrl');if(!url)return showStatus('Add a Suno URL first.');S.regular.push({id:'suno-'+Date.now(),label:val('sunoLabel')||'Scheduled Suno',type:'suno',time:val('sunoTime')||'14:00',url,enabled:true});S.regular=S.regular.sort(scheduleSort);save(true);}; document.querySelectorAll('[data-reg]').forEach(b=>b.onclick=()=>{S.regular.splice(Number(b.dataset.reg),1);save(true);}); document.querySelectorAll('[data-toggle-reg]').forEach(b=>b.onclick=()=>{const r=S.regular[Number(b.dataset.toggleReg)];if(r)r.enabled=!r.enabled;save(true);}); if($('poolOpen'))$('poolOpen').value=S.poolOpen; if($('poolClose'))$('poolClose').value=S.poolClose; if($('autoStart'))$('autoStart').value=String(!!S.autoStart); if($('autoStop'))$('autoStop').value=String(!!S.autoStop); if($('saveHours'))$('saveHours').onclick=()=>{S.poolOpen=val('poolOpen');S.poolClose=val('poolClose');S.autoStart=val('autoStart')==='true';S.autoStop=val('autoStop')==='true';S.regular=S.regular.sort(scheduleSort);save(true);}; if($('verify'))$('verify').onclick=verify; if($('device'))$('device').onclick=deviceLoc; if($('weatherCheck'))$('weatherCheck').onclick=weatherCheck; if($('saveLoc'))$('saveLoc').onclick=()=>{S.address=val('address');S.lat=val('lat');S.lon=val('lon');S.radius=Number(val('radius'))||10;save(true);}; if($('voiceMode'))$('voiceMode').value=S.voiceMode; if($('aiVoice'))$('aiVoice').value=S.aiVoice; if($('saveVoice'))$('saveVoice').onclick=()=>{S.voiceMode=val('voiceMode');S.aiVoice=val('aiVoice');S.deviceVoice=val('deviceVoice');S.voiceRate=Number(val('rate'))||0.94;S.voicePitch=Number(val('pitch'))||1;save(true);}; if($('testVoice'))$('testVoice').onclick=()=>speak('This is Serenity Shores Poolside Pulse version '+VERSION+'. This is a natural voice test for resort announcements.',{}); }
function loadVoices(){voices=('speechSynthesis'in window)?speechSynthesis.getVoices():[];render();}
if('speechSynthesis'in window){speechSynthesis.onvoiceschanged=loadVoices;voices=speechSynthesis.getVoices();}
render();pullState();setInterval(pullState,2000);setInterval(scheduleTick,15000);setInterval(weatherCheck,5*60*1000);
