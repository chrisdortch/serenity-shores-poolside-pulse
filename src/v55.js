const VERSION='5.5';
const PIN='7900';
const KEY='poolside-pulse-v55';
const SPOTIFY_TOKEN_KEY='poolside-pulse-v55-spotify-token';
const DEVICE_KEY='poolside-pulse-v55-device-id';
const MIGRATE=['poolside-pulse-v5','poolside-pulse-v49','poolside-pulse-v44','poolside-pulse-v4','poolside-pulse-v39','poolside-pulse-v38','poolside-pulse-v36-safe'];
const API={state:'/api/state',suno:'/api/suno-playlist?url=',tts:'/api/tts',voiceHealth:'/api/voice-health',weather:'/api/weather',geo:'/api/geocode?address='};
const PROVIDERS={suno:'Suno',spotify:'Spotify'};
const DEFAULT_PLAYLIST='https://suno.com/playlist/cf4b536e-9005-4c98-9ea5-a7f01eca116f';
const DEFAULT_ADDRESS='615 Serenity Shores Ln, Kimberling City, MO 65686';
const DEFAULT_SPOTIFY_CLIENT_ID='7e086716aaea4ce98051287b552a676c';
const DEFAULT_ANNS=[
 ['welcome','Welcome','Good morning and welcome to Serenity Shores. We are glad your family is here. Please supervise children, keep glass out of the pool area, and follow lifeguard instructions so everyone can enjoy a safe day by the water.'],
 ['owner','Owner Message','From all of us at Serenity Shores, thank you for spending part of your vacation with us. We hope this place feels peaceful, fun, and memorable for your family.'],
 ['manager','Manager Message','Friendly Serenity Shores reminder: safety comes first, children must be supervised, and lifeguard instructions should be followed right away.'],
 ['birthday','Birthday','Happy birthday, {name}, from Serenity Shores. We hope your day is absolutely wonderful.'],
 ['close15','Closing 15','Attention guests, the pool will close in 15 minutes. Please begin gathering your belongings.'],
 ['close5','Closing 5','Attention guests, the pool will close in 5 minutes. Thank you for spending the day at Serenity Shores.'],
 ['umbrellas','Close Umbrellas','Attention guests, please close all umbrellas and secure loose items. Thank you.'],
 ['lightning','Lightning Hold','Attention guests and lifeguards. Lightning has been detected within ten miles of Serenity Shores. Please exit the pool and clear the water now. The pool must remain clear for 30 minutes. If another strike is detected, that 30 minute safety clock resets. Lifeguards will announce when the pool may reopen.'],
 ['wind','Strong Wind','Attention guests and lifeguards. Strong wind has been detected near Serenity Shores. Please close umbrellas, secure loose items, and follow staff instructions.'],
 ['lightningclear','Lightning All Clear','Attention guests and lifeguards. The lightning safety hold has ended. Lifeguards may reopen the pool when the area is ready and conditions remain safe.'],
 ['hydrate','Hydration','Friendly reminder from Serenity Shores: take a water break, reapply sunscreen, and keep an eye on younger swimmers.'],
 ['noglass','No Glass','Friendly reminder: glass is not permitted in the pool area. Thank you for helping us keep the pool safe for everyone.'],
 ['safety','Safety Hold','Attention guests, we are taking a safety hold. Please clear the pool and follow lifeguard instructions.'],
 ['weatherwatch','Weather Watch','Attention guests, weather is being monitored near Serenity Shores. Please stay alert for instructions from lifeguards.']
].map(([id,label,text])=>({id,label,text}));
const DEFAULT_SCHEDULE=[
 {id:'open',label:'Pool Open Welcome',type:'announcement',time:'09:05',announcementId:'welcome',enabled:true},
 {id:'ten',label:'10am Welcome',type:'announcement',time:'10:00',announcementId:'welcome',enabled:true},
 {id:'lunch',label:'Lunch Safety Reminder',type:'announcement',time:'12:30',announcementId:'noglass',enabled:true},
 {id:'hydrate',label:'Afternoon Hydration',type:'announcement',time:'15:00',announcementId:'hydrate',enabled:true},
 {id:'close15',label:'Closing 15',type:'announcement',offsetToClose:15,announcementId:'close15',enabled:true},
 {id:'close5',label:'Closing 5',type:'announcement',offsetToClose:5,announcementId:'close5',enabled:true}
];
const BASE={screen:'home',admin:false,tab:'command',revision:0,sync:false,syncMode:'starting',musicProvider:'suno',playlistUrl:DEFAULT_PLAYLIST,spotifyUrl:'',spotifyClientId:DEFAULT_SPOTIFY_CLIENT_ID,playlistName:'Serenity Shores Poolside Pulse',tracks:[{title:'Import the Serenity Shores playlist',artist:'Poolside Pulse',duration:'3:00',audioUrl:''}],current:0,intent:'stopped',command:null,announcement:null,lastError:'',feedback:'Ready.',anns:DEFAULT_ANNS,schedule:DEFAULT_SCHEDULE,selected:'welcome',editId:'new',quickText:'Attention guests, this is a Serenity Shores pool announcement.',guestName:'',poolOpen:'09:00',poolClose:'22:00',playbackMode:'always',autoStart:true,autoStop:false,weather:'Weather monitor is standing by.',weatherCheckedAt:'',weatherAuto:true,weatherLastThreatKey:'',address:DEFAULT_ADDRESS,lat:'36.6337',lon:'-93.4166',radius:10,windGustMph:35,voiceMode:'ai',aiVoice:'marin',deviceVoice:'',voiceHealth:'Not checked yet.',rate:.94,pitch:1,lastRun:{},activityLog:[]};
let S=loadState();
S=v5State(S);
let music=new Audio();
let receiverActive=false;
let speaking=false;
let lastCommandId='';
let lastAnnouncementId='';
let voices=[];
let queue=[];
let commandPage;
music.preload='auto';
music.volume=.95;
music.onended=()=>nextTrack(false,'Track ended; advancing.');
music.onerror=()=>nextTrack(false,'Track failed; trying next playable track.');
function clone(x){return JSON.parse(JSON.stringify(x));}
function $(id){return document.getElementById(id);}
function val(id){return $(id)?.value||'';}
function esc(x){return String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
function uid(){return Date.now()+'-'+Math.random().toString(36).slice(2);}
function stamp(){return new Date().toLocaleString([],{month:'short',day:'numeric',hour:'numeric',minute:'2-digit',second:'2-digit'});}
function deviceId(){let id=localStorage.getItem(DEVICE_KEY);if(!id){id='dev-'+Math.random().toString(36).slice(2,8);localStorage.setItem(DEVICE_KEY,id);}return id;}
function deviceRole(){return S.screen==='home'?'receiver':'command';}
function deviceLabel(){return `${deviceRole()} ${deviceId().slice(-4)}`;}
function logEvent(kind,title,detail='',extra={}){const entry={id:uid(),ts:Date.now(),time:stamp(),kind,title,detail,device:deviceLabel(),...extra};S.activityLog=[entry,...(Array.isArray(S.activityLog)?S.activityLog:[])].slice(0,120);localSave();return entry;}
async function issueCommand(type,payload={},message='Command sent to receivers.'){S.command={id:uid(),type,createdAt:Date.now(),source:deviceLabel(),...payload};logEvent('command',payload.label||type,payload.detail||'',{commandType:type});await pushState(message);render();}
function setFeedback(msg,ok=true){S.feedback=`${ok?'✅':'⚠️'} ${msg} · ${stamp()}`;if(!ok)S.lastError=msg;localSave();const el=$('feedbackBox');if(el)el.innerHTML=`<b>Status:</b> ${esc(S.feedback)}`;}
function loadState(){for(const k of [KEY,...MIGRATE]){try{const raw=localStorage.getItem(k);if(raw)return normalize(JSON.parse(raw));}catch{}}return normalize({});}
function normalize(raw){const s={...clone(BASE),...(raw||{})};if(raw?.rev&&!raw.revision)s.revision=raw.rev;if(raw?.cmd&&!raw.command)s.command=raw.cmd;if(raw?.announce&&!raw.announcement)s.announcement=raw.announce;if(!PROVIDERS[s.musicProvider])s.musicProvider='suno';if(!['always','hours'].includes(s.playbackMode))s.playbackMode='always';s.windGustMph=Math.max(15,Math.min(80,Number(s.windGustMph)||35));s.radius=Math.max(1,Math.min(25,Number(s.radius)||10));s.anns=Array.isArray(s.anns)?s.anns:clone(DEFAULT_ANNS);s.schedule=Array.isArray(s.schedule)?s.schedule:clone(DEFAULT_SCHEDULE);const aids=new Set(s.anns.map(a=>a.id));DEFAULT_ANNS.forEach(a=>{if(!aids.has(a.id))s.anns.push(clone(a));});const rids=new Set(s.schedule.map(r=>r.id));DEFAULT_SCHEDULE.forEach(r=>{if(!rids.has(r.id))s.schedule.push(clone(r));});if(!Array.isArray(s.tracks)||!s.tracks.length)s.tracks=clone(BASE.tracks);if(!s.selected||!s.anns.find(a=>a.id===s.selected))s.selected=s.anns[0].id;if(!s.editId)s.editId='new';s.current=Math.max(0,Math.min(Number(s.current)||0,Math.max(0,s.tracks.length-1)));return s;}
function localSave(){localStorage.setItem(KEY,JSON.stringify(S));}
function cloudState(){const c={...clone(S),screen:'home',admin:false,tab:'music',editId:'new'};if(S.screen!=='home'){delete c.intent;delete c.current;}return c;}
async function pushState(message='Saved and shared.'){try{const r=await fetch(API.state,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({state:cloudState()})});const d=await r.json().catch(()=>({}));if(!r.ok||d.ok===false)throw Error(d.error||`State HTTP ${r.status}`);S.sync=!!d.cloudSync;S.syncMode=d.syncMode||'server';if(d.state?.revision)S.revision=d.state.revision;localSave();if(message)setFeedback(message,true);return true;}catch(e){S.sync=false;setFeedback(`State sync failed: ${e.message}`,false);return false;}}
async function pullState(){try{const r=await fetch(API.state,{cache:'no-store'});const d=await r.json().catch(()=>({}));S.sync=!!d.cloudSync;S.syncMode=d.syncMode||'server';const inc=d.state;if(!inc||Number(inc.revision||0)<=Number(S.revision||0)){return;}const newCommand=inc.command?.id&&inc.command.id!==lastCommandId;const newAnnouncement=inc.announcement?.id&&inc.announcement.id!==lastAnnouncementId;const wasReceiver=S.screen==='home'&&receiverActive;const old={screen:S.screen,admin:S.admin,tab:S.tab,selected:S.selected,editId:S.editId,feedback:S.feedback,lastError:S.lastError,intent:S.intent,current:S.current};S=normalize({...S,...inc,revision:inc.revision});S.screen=old.screen;S.admin=old.admin;S.tab=old.tab;S.selected=old.selected;S.editId=old.editId;S.feedback=old.feedback;S.lastError=old.lastError;if(wasReceiver&&!newCommand){S.intent=old.intent;S.current=old.current;}localSave();await applyRemote(newCommand,newAnnouncement);render();}catch{S.sync=false;localSave();}}
async function save(message='Saved.'){localSave();await pushState(message);render();}
function track(){return S.tracks[S.current]||S.tracks[0]||BASE.tracks[0];}
function hasPlayable(){return(S.tracks||[]).some(t=>t.audioUrl);}
function nextPlayableIndex(from=S.current){if(!S.tracks?.length)return-1;for(let i=1;i<=S.tracks.length;i++){const n=(Number(from)+i+S.tracks.length)%S.tracks.length;if(S.tracks[n]?.audioUrl)return n;}return-1;}
function readMusicSettings(){S.musicProvider=val('musicProvider')||S.musicProvider;S.playlistName=val('playlistName')||S.playlistName;S.playlistUrl=val('playlistUrl')||S.playlistUrl;S.spotifyUrl=val('spotifyUrl')||S.spotifyUrl;if(!PROVIDERS[S.musicProvider])S.musicProvider='suno';localSave();}
async function importSuno(message='Suno playlist imported and published.'){readMusicSettings();S.musicProvider='suno';setFeedback('Importing Suno playlist...',true);render();try{const r=await fetch(API.suno+encodeURIComponent(S.playlistUrl));const d=await r.json().catch(()=>({}));if(!r.ok||!d.tracks?.length)throw Error(d.error||`Import HTTP ${r.status}`);S.tracks=d.tracks.map(x=>({title:x.title||'Untitled',artist:x.artist||'Suno',duration:x.duration||'3:00',audioUrl:x.audioUrl||'',sourceUrl:x.sourceUrl||S.playlistUrl}));const first=S.tracks.findIndex(x=>x.audioUrl);S.current=first>=0?first:0;S.lastError=d.audioWarning||'';await save(`${message} ${S.tracks.length} track(s) loaded.`);}catch(e){setFeedback(`Suno import failed: ${e.message}`,false);render();}}
async function fade(media,to,ms=650){const start=Number.isFinite(media.volume)?media.volume:.95;const steps=16;for(let i=1;i<=steps;i++){media.volume=Math.max(0,Math.min(1,start+(to-start)*(i/steps)));await new Promise(r=>setTimeout(r,ms/steps));}}
function sameUrl(a,b){try{return new URL(a,location.href).href===new URL(b,location.href).href;}catch{return String(a||'')===String(b||'');}}
function providerLabel(){return PROVIDERS[S.musicProvider]||'Suno';}
function activeProviderUrl(){return S.musicProvider==='spotify'?S.spotifyUrl:S.playlistUrl;}
function controlledProvider(){return S.musicProvider==='suno';}
function providerNotice(){return controlledProvider()?'Suno/direct audio is controlled by Poolside Pulse, so music can fade down before announcements.':'This provider opens in its official player. Poolside Pulse can play announcements at full app volume, but browser security may prevent ducking audio that plays in another app or cross-origin player.';}
function openProvider(push=false){const url=activeProviderUrl();if(!url){setFeedback(`Paste a ${providerLabel()} playlist URL first.`,false);return;}window.open(url,'_blank','noopener,noreferrer');S.intent='external';setFeedback(`${providerLabel()} opened. Keep this iPhone volume high; use Poolside Pulse for announcements and weather alerts.`,true);if(push){S.command={id:uid(),type:'provider',provider:S.musicProvider,url,createdAt:Date.now()};pushState(`${providerLabel()} player opened on Home receivers.`);}render();}
function shouldPlayContinuously(){return S.playbackMode==='always'||openNow();}
function shouldResumeMusic(){return S.playbackMode==='always'||openNow();}
function trackOptions(selected){return(S.tracks||[]).map((t,i)=>`<option value="${i}" ${Number(selected)===i?'selected':''}>${i+1}. ${esc(t.title||'Untitled')} - ${esc(t.artist||'')}</option>`).join('')||'<option value="0">Import a queue first</option>';}
async function play(push=true){receiverActive=true;if(!controlledProvider()){openProvider(push);return;}if(!hasPlayable()){await importSuno('Playlist auto-imported for receiver.');if(!hasPlayable()){S.intent='stopped';setFeedback('No playable Suno/direct audio loaded. Import the playlist first.',false);if(push)await pushState('Play command sent, but no playable audio is loaded.');render();return;}}if(!track().audioUrl){const n=nextPlayableIndex(S.current-1);if(n>=0)S.current=n;}const url=track().audioUrl;if(!sameUrl(music.src,url))music.src=url;try{music.volume=Math.max(music.volume||.02,.02);music.muted=false;await music.play();await fade(music,.95,650);S.intent='playing';setFeedback(`Receiver playing: ${track().title}`,true);}catch(e){S.intent='stopped';setFeedback(`Browser blocked audio. Click Activate Sound / Play Station on this Home receiver. ${e.message||''}`,false);}if(push){S.command={id:uid(),type:'play',createdAt:Date.now()};await pushState('Play command sent to all Home receivers.');}render();}
async function pause(push=true){await fade(music,0,300);music.pause();music.volume=.95;S.intent='paused';setFeedback('Paused.',true);if(push){S.command={id:uid(),type:'pause'};await pushState('Pause command sent to receivers.');}render();}
async function stop(push=true){await fade(music,0,300);music.pause();music.currentTime=0;music.volume=.95;S.intent='stopped';setFeedback('Stopped.',true);if(push){S.command={id:uid(),type:'stop'};await pushState('Stop command sent to receivers.');}render();}
async function nextTrack(push=true,message='Skipped to next playable track.'){if(!controlledProvider()){setFeedback(`Use the ${providerLabel()} player to skip tracks.`,false);return;}const n=nextPlayableIndex(S.current);if(n<0){S.intent='stopped';setFeedback('No next playable track found.',false);if(push)await pushState('Skip command sent, but no next playable track exists.');render();return;}S.current=n;const url=track().audioUrl;if(!sameUrl(music.src,url))music.src=url;if(S.intent==='playing'||receiverActive){try{music.muted=false;music.volume=Math.max(music.volume||.02,.02);await music.play();S.intent='playing';setFeedback(message,true);}catch(e){setFeedback(`Next track blocked. ${e.message||'Tap Play on receiver.'}`,false);}}if(push){S.command={id:uid(),type:'skip'};await pushState('Skip command sent to receivers.');}render();}
function mins(x){const[h,m]=String(x||'0:0').split(':').map(Number);return(h||0)*60+(m||0);}
function hm(n){n=((Number(n)||0)%1440+1440)%1440;return String(Math.floor(n/60)).padStart(2,'0')+':'+String(n%60).padStart(2,'0');}
function pretty(x){const[h,m]=String(x||'00:00').split(':').map(Number);return`${((h+11)%12)+1}:${String(m||0).padStart(2,'0')} ${h>=12?'PM':'AM'}`;}
function schedTime(item){return Number.isFinite(Number(item.offsetToClose))?hm(mins(S.poolClose)-Number(item.offsetToClose)):(item.time||'12:00');}
function today(){return new Date().toISOString().slice(0,10);}
function openNow(){const m=new Date().getHours()*60+new Date().getMinutes(),a=mins(S.poolOpen),b=mins(S.poolClose);return a<=b?m>=a&&m<b:m>=a||m<b;}
function ann(){return S.anns.find(x=>x.id===S.selected)||S.anns[0];}
function annText(id){return(S.anns.find(x=>x.id===id)||{}).text||'';}
function itemBody(item){if(item.type==='suno')return item.url||'';if(item.type==='song'){const t=S.tracks?.[Number(item.trackIndex)||0];return t?`Specific song: ${t.title}`:'Specific song from current queue';}return item.announcementId?annText(item.announcementId):(item.text||'');}
function tokens(x){return String(x||'').replaceAll('{name}',S.guestName||'friend').replaceAll('{close}',pretty(S.poolClose)).replaceAll('{open}',pretty(S.poolOpen)).replaceAll('{resort}','Serenity Shores');}
function musicAudible(){return receiverActive&&!music.paused&&!!music.src&&music.volume>0;}
function bestVoices(){return voices.filter(v=>/^en/i.test(v.lang||'')).sort((a,b)=>score(b)-score(a)).slice(0,30);}
function score(v){const n=(v.name+' '+v.lang).toLowerCase();let s=0;['samantha','ava','allison','siri','google','microsoft','aria','jenny','natural','premium','enhanced','serena'].forEach((p,i)=>{if(n.includes(p))s+=60-i;});if(n.includes('en-us'))s+=10;return s;}
async function speak(text,hold=false){const msg=tokens(text).trim();if(!msg){setFeedback('Announcement text is empty.',false);return;}if(speaking){queue.push({text:msg,hold});setFeedback('Announcement queued behind current announcement.',true);return;}speaking=true;const hadMusic=musicAudible();if(hadMusic){setFeedback('Announcement starting; music fading to zero without restarting track.',true);await fade(music,0,900);try{music.pause();}catch{}}else if(!controlledProvider()&&S.intent==='external'){setFeedback(`${providerLabel()} is outside app volume control. Announcement will play at full app volume.`,true);}let spoken=false;const finish=async()=>{speaking=false;if(hadMusic&&!hold&&shouldResumeMusic()){try{music.muted=false;music.volume=Math.max(music.volume||.02,.02);await music.play();await fade(music,.95,1050);S.intent='playing';setFeedback('Announcement finished; same track faded back up without restart.',true);}catch(e){setFeedback(`Could not fade music back up: ${e.message}`,false);}}else if(hold&&hadMusic){music.pause();S.intent='paused';setFeedback('Safety hold finished; music remains paused.',true);}localSave();render();const next=queue.shift();if(next)setTimeout(()=>speak(next.text,next.hold),250);};
if(S.voiceMode==='ai'){try{const r=await fetch(API.tts,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text:msg,voice:S.aiVoice||'marin',instructions:'Speak like a calm, polished, natural resort public-address announcer at Serenity Shores. Be clear, warm, and firm for safety.'})});if(r.ok){const blob=await r.blob();const url=URL.createObjectURL(blob);const a=new Audio(url);a.preload='auto';a.playsInline=true;a.muted=false;a.volume=1;a.onended=()=>{URL.revokeObjectURL(url);finish();};a.onerror=()=>{URL.revokeObjectURL(url);setFeedback('AI announcement audio failed during playback.',false);finish();};await a.play();spoken=true;setFeedback(`AI voice playing at full app volume: ${S.aiVoice}.`,true);}else{const d=await r.json().catch(()=>({}));setFeedback(`AI voice failed ${r.status}: ${d.error||d.detail||'check OpenAI key/billing/limits'}. Device fallback used.`,false);}}catch(e){setFeedback(`AI voice error: ${e.message}. Device fallback used.`,false);}}
if(!spoken){if('speechSynthesis'in window){speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(msg);const v=voices.find(v=>v.name===S.deviceVoice)||bestVoices()[0];if(v)u.voice=v;u.rate=Number(S.rate)||.94;u.pitch=Number(S.pitch)||1;u.volume=1;u.onend=finish;u.onerror=finish;speechSynthesis.speak(u);setFeedback(`Device voice speaking at full app volume${v?': '+v.name:''}.`,true);}else{setFeedback('No speech engine available in this browser.',false);await finish();}}}
async function sendAnnouncement(text,hold=false){const msg=tokens(text).trim();if(!msg){setFeedback('Nothing to announce. Type text first.',false);return;}const id=uid();S.announcement={id,text:msg,hold,createdAt:Date.now()};if(S.screen==='home')lastAnnouncementId=id;await pushState('Announcement command sent to all active Home receivers.');if(S.screen==='home')await speak(msg,hold);render();}
async function playScheduleItem(item){if(!item){setFeedback('No schedule item selected.',false);return;}if(item.type==='song'){const index=Math.max(0,Math.min(Number(item.trackIndex)||0,(S.tracks||[]).length-1));S.current=index;S.command={id:uid(),type:'song',trackIndex:index,label:item.label,createdAt:Date.now()};await pushState(`Specific song sent to receivers: ${track().title}`);if(S.screen==='home')await play(false);render();return;}if(item.type==='suno'){if(!item.url){setFeedback('This schedule item has no Suno URL.',false);return;}S.command={id:uid(),type:'suno',url:item.url,label:item.label,createdAt:Date.now()};await pushState(`Suno item sent to receivers: ${item.label}`);if(S.screen==='home'){S.musicProvider='suno';S.playlistUrl=item.url;await importSuno('Scheduled Suno URL imported.');await play(false);}render();}else{await sendAnnouncement(itemBody(item),!!item.hold);}}
async function applyRemote(newCommand,newAnnouncement){if(S.screen!=='home')return;if(newCommand&&S.command?.id&&S.command.id!==lastCommandId&&!speaking){lastCommandId=S.command.id;if(S.command.type==='play')await play(false);if(S.command.type==='pause')await pause(false);if(S.command.type==='stop')await stop(false);if(S.command.type==='skip')await nextTrack(false);if(S.command.type==='provider'){S.musicProvider=S.command.provider||S.musicProvider;if(S.command.provider==='spotify')S.spotifyUrl=S.command.url||S.spotifyUrl;openProvider(false);}if(S.command.type==='song'){S.current=Math.max(0,Math.min(Number(S.command.trackIndex)||0,(S.tracks||[]).length-1));await play(false);}if(S.command.type==='suno'){S.musicProvider='suno';S.playlistUrl=S.command.url;await importSuno('Remote Suno URL imported.');await play(false);}}if(newAnnouncement&&S.announcement?.id&&S.announcement.id!==lastAnnouncementId){lastAnnouncementId=S.announcement.id;await speak(S.announcement.text,S.announcement.hold);}}
async function tick(){if(S.screen==='home'&&receiverActive&&!speaking){if(S.autoStart&&controlledProvider()&&shouldPlayContinuously()&&S.intent!=='playing')await play(false);if(S.autoStop&&S.playbackMode==='hours'&&!openNow()&&S.intent==='playing')await stop(false);const now=hm(new Date().getHours()*60+new Date().getMinutes());for(const it of S.schedule){if(!it.enabled||schedTime(it)!==now)continue;const key=today()+':'+it.id+':'+schedTime(it);if(S.lastRun[it.id]===key)continue;S.lastRun[it.id]=key;localSave();await playScheduleItem(it);}}}
function parseCoord(x){const n=Number(String(x||'').trim().replace(/[−–—]/g,'-'));return Number.isFinite(n)?n:null;}
function weatherAnnouncement(d){if(/wind/i.test(d.threatType||''))return`Attention guests. Strong wind has been detected near Serenity Shores. Please close umbrellas, secure loose items, and follow staff instructions immediately.`;if(/tornado/i.test(d.threatType||''))return'Attention guests. A tornado warning has been detected near Serenity Shores. Exit the pool area and move indoors immediately.';return'Attention guests. Lightning or severe weather activity has been detected nearby. Please exit the pool area and move indoors immediately.';}
async function weather(){const lat=parseCoord(S.lat),lon=parseCoord(S.lon),radius=Math.max(1,Math.min(25,Number(S.radius)||10)),windGustMph=Math.max(15,Math.min(80,Number(S.windGustMph)||35));if(lat===null||lon===null){S.weather='Enter valid latitude and longitude, or tap Use Device GPS.';S.weatherCheckedAt=stamp();setFeedback('Weather check failed: invalid coordinates.',false);await save('Weather settings saved, but coordinates are invalid.');return;}try{const params=new URLSearchParams({lat:String(lat),lon:String(lon),radiusMiles:String(radius),windGustMph:String(windGustMph)});const r=await fetch(`${API.weather}?${params.toString()}`);const d=await r.json().catch(()=>({}));if(!r.ok||d.ok===false)throw Error(d.error||`Weather HTTP ${r.status}`);S.weather=d.summary||'Weather checked.';S.weatherCheckedAt=stamp();S.lat=String(lat);S.lon=String(lon);S.radius=radius;S.windGustMph=windGustMph;if(d.threat){const threatKey=`${today()}:${d.threatType}:${String(d.summary||'').slice(0,120)}`;if(S.weatherLastThreatKey!==threatKey){S.weatherLastThreatKey=threatKey;await sendAnnouncement(weatherAnnouncement(d),true);}else{setFeedback('Weather threat still present; duplicate announcement suppressed for this matching alert.',true);}}await save('Weather checked and saved.');}catch(e){S.weather=`Weather check failed: ${e.message}`;S.weatherCheckedAt=stamp();setFeedback(`Weather check failed: ${e.message}`,false);await save('Weather failure saved for screenshot troubleshooting.');}}
async function geocode(){S.address=val('address')||S.address;try{const r=await fetch(API.geo+encodeURIComponent(S.address));const d=await r.json().catch(()=>({}));if(!r.ok||d.ok===false)throw Error(d.error||`Geocode HTTP ${r.status}`);S.lat=String(d.latitude);S.lon=String(d.longitude);await save(`Location verified: ${d.matchedAddress||S.address}`);}catch(e){setFeedback(`Could not verify address: ${e.message}. Use Device GPS or enter coordinates.`,false);render();}}
function useGps(){if(!navigator.geolocation){setFeedback('Device GPS is not available in this browser.',false);return;}setFeedback('Requesting device GPS permission...',true);navigator.geolocation.getCurrentPosition(async p=>{S.lat=p.coords.latitude.toFixed(6);S.lon=p.coords.longitude.toFixed(6);await save(`Device GPS filled: ${S.lat}, ${S.lon} (accuracy about ${Math.round(p.coords.accuracy||0)}m).`);},err=>setFeedback(`Device GPS failed: ${err.message}`,false),{enableHighAccuracy:true,timeout:12000,maximumAge:60000});}
function header(){return `<header class="top"><div class="brand"><b>SS</b><span>Serenity Shores<small>Poolside Pulse · V${VERSION}</small></span></div><div class="tog"><button id="home" class="${S.screen==='home'?'active':''}">Home</button><button id="cmd" class="${S.screen!=='home'?'active':''}">Command</button></div></header>`;}
function nav(){return `<nav class="tabs stickyTabs">${[['music','Music'],['schedule','Announcement Schedule'],['hours','Hours'],['weather','Weather'],['voice','Voice']].map(x=>`<button data-tab="${x[0]}" class="${S.tab===x[0]?'on':''}">${x[1]}</button>`).join('')}</nav>`;}
function shell(body){return `${header()}<main class="wrap"><div class="version">Command Center · Version ${VERSION} · ${S.sync?'Sync Active':'Preview Sync'} · ${esc(S.syncMode||'')}</div>${nav()}<div id="feedbackBox" class="status"><b>Status:</b> ${esc(S.feedback||'Ready.')}</div>${S.lastError?`<div class="status"><b>Last Error:</b> ${esc(S.lastError)}</div>`:''}${body}</main>`;}
function homePage(){const x=track();const next=S.schedule.filter(x=>x.enabled).sort((a,b)=>mins(schedTime(a))-mins(schedTime(b))).slice(0,5).map(x=>`<p class="line"><b>${pretty(schedTime(x))}</b><span>${esc(x.label)}</span></p>`).join('');return `${header()}<main class="home"><section class="hero compact"><div><p class="tag">Private Resort Station · V${VERSION}</p><h1>Poolside Pulse</h1><p class="sub">Always-on resort music, loud spoken announcements, scheduled songs, and weather-aware pool safety.</p><div class="actions"><button id="playHome">${S.intent==='playing'?'Pause':'Activate Sound / Play Station'}</button><button id="skipHome" class="secondary">Skip</button><button id="stopHome" class="secondary">Stop</button></div><p class="mini">${receiverActive?'Receiver active':'Tap once on this iPhone/browser'} · ${providerLabel()} · ${S.playbackMode==='always'?'always-on':'pool-hours'} · weather every 5 minutes</p></div><aside><b>Weather</b><p>${esc(S.weather)}</p><small>${esc(S.weatherCheckedAt?'Last checked '+S.weatherCheckedAt:'Not checked yet')} · wind ${esc(S.windGustMph)} mph</small><br><br><b>Pool Hours</b><p>${pretty(S.poolOpen)} - ${pretty(S.poolClose)}</p></aside></section><section class="now"><div class="art">SS<span>Pulse</span></div><div><p class="tag teal">${S.intent==='playing'?'Now Playing':S.intent==='external'?'External Player':'Station Ready'}</p><h2>${esc(controlledProvider()?x.title:providerLabel())}</h2><p>${esc(controlledProvider()?`${x.artist} · ${x.duration}`:activeProviderUrl()||'Paste a provider playlist URL in Command.')}</p><div class="bar"><i></i></div><p>${esc(S.playlistName)}</p><p class="mini dark">${esc(S.feedback||'')}</p>${S.lastError?`<p class="warn">${esc(S.lastError)}</p>`:''}</div></section><section class="cards"><div class="card"><h3>Next Scheduled</h3>${next||'<p>No enabled schedule items.</p>'}</div><div class="card"><h3>Weather Monitor</h3><p>${esc(S.weather)}</p></div></section></main>`;}
function login(){return `${header()}<main class="wrap"><section class="card login"><h1>Command Center</h1><input id="pin" inputmode="numeric" type="password" placeholder="PIN"><button id="login">Enter</button></section></main>`;}
function musicPage(){const q=S.tracks.map((x,i)=>`<div class="q ${i===S.current?'cur':''}"><div><b>${i+1}. ${esc(x.title)}</b><span>${esc(x.artist)} · ${x.audioUrl?'playable':'title only'}</span></div><button data-song="${i}">Play</button></div>`).join('');const providers=Object.entries(PROVIDERS).map(([id,label])=>`<option value="${id}" ${S.musicProvider===id?'selected':''}>${label}</option>`).join('');return shell(`<section class="panel"><h1>Music Source</h1><p>Provider: ${esc(providerLabel())}. ${esc(providerNotice())}</p><div class="grid2"><label>Music Provider<select id="musicProvider">${providers}</select></label><label>Playlist Name<input id="playlistName" value="${esc(S.playlistName)}"></label></div><label>Suno Playlist URL<input id="playlistUrl" value="${esc(S.playlistUrl)}"></label><label>Spotify Playlist URL<input id="spotifyUrl" value="${esc(S.spotifyUrl)}" placeholder="https://open.spotify.com/playlist/..."></label><div class="actions"><button id="saveMusic">Save Music Settings</button><button id="import" class="secondary">Import Suno Queue</button><button id="openProvider" class="secondary">Open Provider Player</button><button id="playCmd" class="secondary">Play Receivers</button><button id="pauseCmd" class="secondary">Pause</button><button id="skipCmd" class="secondary">Skip</button><button id="stopCmd" class="secondary">Stop</button></div></section><section class="panel"><h2>Published Queue</h2>${q}</section>`);}
function schedulePage(){const selected=ann();const opts=S.anns.map(a=>`<option value="${a.id}">${esc(a.label)}</option>`).join('');const rows=S.schedule.map((r,i)=>({r,i})).sort((a,b)=>mins(schedTime(a.r))-mins(schedTime(b.r))).map(({r,i})=>{const active=S.editId===r.id;const mode=r.type==='spotify'?'spotify':r.type==='song'?'song':r.type==='suno'?'suno':(r.announcementId?'reusable':'custom');const edit=`<div class="status" style="${active?'border:3px solid #10223f;background:#f8fdff':''}"><div class="grid2"><label>Type<select data-kind="${i}"><option value="reusable" ${mode==='reusable'?'selected':''}>Saved announcement</option><option value="custom" ${mode==='custom'?'selected':''}>Custom text or Spotify URL</option><option value="song" ${mode==='song'?'selected':''}>Specific loaded song</option><option value="suno" ${mode==='suno'?'selected':''}>Suno URL</option></select></label><label>Label<input data-label="${i}" value="${esc(r.label)}"></label></div><label>Custom Text, Spotify URL, or Suno URL<textarea data-body="${i}">${esc(r.type==='spotify'||r.type==='suno'?r.url:(r.announcementId||r.type==='song'?'':r.text||''))}</textarea></label><label>Specific Loaded Song<select data-track="${i}">${trackOptions(r.trackIndex)}</select></label><label>Saved Announcement<select data-selann="${i}">${opts}</select></label><button data-save-row="${i}">Save Edited Item</button></div>`;return `<div class="q ${r.enabled?'':'off'}" style="${active?'border:3px solid #10223f;box-shadow:0 0 0 4px rgba(7,137,165,.12)':''}"><div style="width:100%"><b><input data-time="${i}" type="time" value="${schedTime(r)}" style="max-width:170px;margin-right:8px"> ${esc(r.label)}</b><span>${r.enabled?'enabled':'off'} · ${mode==='spotify'?'Spotify URL':mode==='song'?'specific song':mode==='suno'?'Suno URL':(r.announcementId?'saved announcement':'custom text')}</span><p>${esc(tokens(itemBody(r)))}</p>${active?edit:''}</div><div class="rowBtns"><button data-play-row="${i}">Play</button><button class="${active?'':'secondary'}" style="${active?'background:#10223f;color:white':''}" data-edit="${i}">${active?'Editing':'Edit'}</button><button class="secondary" data-toggle="${i}">${r.enabled?'Off':'On'}</button><button class="secondary" data-delete="${i}">Delete</button></div></div>`;}).join('');return shell(`<section class="panel"><h1>Announcement Center</h1><p>Shortcut announcements, lightning holds, wind alerts, scheduled readings, Spotify links, Suno links, and scheduled songs.</p><div class="chipgrid">${S.anns.map(a=>`<button class="chip ${a.id===selected.id?'on':''}" data-ann="${a.id}">${esc(a.label)}</button>`).join('')}<button id="newAnn" class="chip add">+ Add Template Button</button></div><label>Quick Text-to-Speech Announcement<textarea id="quickText">${esc(S.quickText||selected.text)}</textarea></label><div class="actions"><button id="quickPlay">Play Quick Text Now</button><button id="quickHold" class="secondary">Play as Safety Hold</button><button id="quickSchedule" class="secondary">Add Quick Text to Schedule</button></div><details><summary><b>Edit selected shortcut/template button</b></summary><div class="grid2"><label>Button Name<input id="annLabel" value="${esc(selected.label)}"></label><label>Optional Guest Name<input id="guestName" value="${esc(S.guestName)}" placeholder="Used for {name}"></label></div><label>Template Text<textarea id="annText">${esc(selected.text)}</textarea></label><div class="actions"><button id="saveAnn" class="secondary">Save Template Button</button><button id="deleteAnn" class="secondary">Delete Template Button</button></div></details></section><section class="panel"><div class="actions"><h2 style="margin-right:auto">Daily Schedule</h2><button id="addSched" class="secondary">+ Add</button></div>${rows}</section>`);}
function hoursPage(){return shell(`<section class="panel"><h1>Playback & Hours</h1><div class="grid2"><label>Music Mode<select id="playbackMode"><option value="always">Always play unless paused</option><option value="hours">Follow pool hours</option></select></label><label>Auto Start<select id="autoStart"><option value="true">Yes</option><option value="false">No</option></select></label></div><div class="grid2"><label>Pool Opens<input id="poolOpen" type="time" value="${S.poolOpen}"></label><label>Pool Closes<input id="poolClose" type="time" value="${S.poolClose}"></label></div><label>Auto Stop<select id="autoStop"><option value="true">Yes, stop at closing when following pool hours</option><option value="false">No</option></select></label><button id="saveHours">Save Playback Settings</button></section>`);}
function weatherPage(){return shell(`<section class="panel"><h1>Location & Weather</h1><label>Address<input id="address" value="${esc(S.address)}"></label><div class="actions"><button id="verify">Verify Address</button><button id="gps" class="secondary">Use Device GPS</button><button id="checkWeather" class="secondary">Check Weather</button></div><div class="grid3"><label>Latitude<input id="lat" value="${esc(S.lat)}"></label><label>Longitude<input id="lon" value="${esc(S.lon)}"></label><label>Radius Miles<input id="radius" value="${esc(S.radius)}"></label></div><div class="grid2"><label>Strong Wind Alert MPH<input id="windGustMph" inputmode="numeric" value="${esc(S.windGustMph)}"></label><label>Auto Scan<select id="weatherAuto"><option value="true">Every 5 minutes on Home receiver</option><option value="false">Manual only</option></select></label></div><button id="saveLoc">Save Weather Settings</button><div class="status"><b>Weather:</b> ${esc(S.weather)}<br><b>${esc(S.weatherCheckedAt?'Last checked '+S.weatherCheckedAt:'Not checked yet')}</b></div></section>`);}
function voicePage(){const opts=bestVoices().map(v=>`<option value="${esc(v.name)}" ${v.name===S.deviceVoice?'selected':''}>${esc(v.name)} · ${esc(v.lang)}</option>`).join('');return shell(`<section class="panel"><h1>Voice Studio</h1><p>AI voice uses OpenAI when billing/quota allows it. Every test reports success or the exact failure so a screenshot can diagnose it.</p><label>Voice Mode<select id="voiceMode"><option value="ai">Natural AI first</option><option value="device">Device only</option></select></label><label>AI Voice<select id="aiVoice"><option value="marin">Marin</option><option value="cedar">Cedar</option><option value="coral">Coral</option><option value="nova">Nova</option><option value="sage">Sage</option><option value="shimmer">Shimmer</option><option value="onyx">Onyx</option></select></label><label>Device Voice<select id="deviceVoice"><option value="">Best available</option>${opts}</select></label><div class="grid2"><label>Speed<input id="rate" type="range" min=".75" max="1.15" step=".01" value="${S.rate}"></label><label>Pitch<input id="pitch" type="range" min=".85" max="1.15" step=".01" value="${S.pitch}"></label></div><div class="actions"><button id="saveVoice">Save Voice</button><button id="testVoice" class="secondary">Test Selected Voice</button><button id="testDevice" class="secondary">Test Device Voice</button></div></section>`);}
function render(){try{document.title=`Serenity Shores Poolside Pulse V${VERSION}`;document.getElementById('app').innerHTML=S.screen==='home'?homePage():!S.admin?login():({music:musicPage,schedule:schedulePage,hours:hoursPage,weather:weatherPage,voice:voicePage}[S.tab]||musicPage)();bind();}catch(e){document.getElementById('app').innerHTML=`<main class="wrap"><section class="panel"><h1>Poolside Pulse V${VERSION}</h1><p class="warn">Recovered from runtime error: ${esc(e.message)}</p><button onclick="localStorage.removeItem('${KEY}');location.reload()">Reset local data</button></section></main>`;}}
function bind(){if($('home'))$('home').onclick=()=>{S.screen='home';localSave();render();};if($('cmd'))$('cmd').onclick=()=>{S.screen='command';localSave();render();};if($('login'))$('login').onclick=()=>{if(val('pin')===PIN){S.admin=true;S.screen='command';S.tab='music';setFeedback('Admin unlocked.',true);render();}else setFeedback('Wrong PIN.',false);};document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{S.tab=b.dataset.tab;localSave();render();});if($('playHome'))$('playHome').onclick=()=>S.intent==='playing'?pause(true):play(true);if($('skipHome'))$('skipHome').onclick=()=>nextTrack(true);if($('stopHome'))$('stopHome').onclick=()=>stop(true);if($('musicProvider'))$('musicProvider').value=S.musicProvider;if($('saveMusic'))$('saveMusic').onclick=()=>{readMusicSettings();save('Music provider settings saved.');};if($('import'))$('import').onclick=()=>{S.musicProvider='suno';importSuno();};if($('openProvider'))$('openProvider').onclick=()=>{readMusicSettings();openProvider(true);};if($('playCmd'))$('playCmd').onclick=()=>{readMusicSettings();play(true);};if($('pauseCmd'))$('pauseCmd').onclick=()=>pause(true);if($('skipCmd'))$('skipCmd').onclick=()=>nextTrack(true);if($('stopCmd'))$('stopCmd').onclick=()=>stop(true);document.querySelectorAll('[data-song]').forEach(b=>b.onclick=()=>{S.current=Number(b.dataset.song);S.musicProvider='suno';play(true);});document.querySelectorAll('[data-ann]').forEach(b=>b.onclick=()=>{const a=S.anns.find(x=>x.id===b.dataset.ann);if(a){S.selected=a.id;S.quickText=a.text;setFeedback(`Loaded template: ${a.label}`,true);localSave();render();}});if($('newAnn'))$('newAnn').onclick=()=>{const id='custom-'+uid();S.anns.push({id,label:'New Template',text:'Type announcement text here.'});S.selected=id;S.quickText='Type announcement text here.';save('New announcement template added.');};if($('saveAnn'))$('saveAnn').onclick=()=>{const a=ann();a.label=val('annLabel')||a.label;a.text=val('annText')||a.text;S.guestName=val('guestName');save('Announcement template saved.');};if($('deleteAnn'))$('deleteAnn').onclick=()=>{if(S.anns.length<2){setFeedback('At least one template must remain.',false);return;}S.anns=S.anns.filter(a=>a.id!==S.selected);S.selected=S.anns[0].id;save('Announcement template deleted.');};if($('quickPlay'))$('quickPlay').onclick=()=>{S.quickText=val('quickText');sendAnnouncement(S.quickText,false);};if($('quickHold'))$('quickHold').onclick=()=>{S.quickText=val('quickText');sendAnnouncement(S.quickText,true);};if($('quickSchedule'))$('quickSchedule').onclick=()=>{S.quickText=val('quickText');const it={id:'sched-'+uid(),label:'Custom Announcement',type:'text',time:'10:00',text:S.quickText,enabled:true};S.schedule.push(it);S.editId=it.id;save('Quick text added to daily schedule.');};if($('addSched'))$('addSched').onclick=()=>{const it={id:'sched-'+uid(),label:'New Schedule Item',type:'text',time:'10:00',text:'Type announcement text here.',enabled:true};S.schedule.push(it);S.editId=it.id;save('New schedule item added.');};document.querySelectorAll('[data-time]').forEach(input=>input.onchange=()=>{const r=S.schedule[Number(input.dataset.time)];delete r.offsetToClose;r.time=input.value;save(`Time updated for ${r.label}.`);});document.querySelectorAll('[data-play-row]').forEach(b=>b.onclick=()=>playScheduleItem(S.schedule[Number(b.dataset.playRow)]));document.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>{const r=S.schedule[Number(b.dataset.edit)];S.editId=S.editId===r.id?'new':r.id;setFeedback(S.editId==='new'?'Edit closed.':`Editing ${r.label}.`,true);localSave();render();});document.querySelectorAll('[data-toggle]').forEach(b=>b.onclick=()=>{const r=S.schedule[Number(b.dataset.toggle)];r.enabled=!r.enabled;save(`${r.label} turned ${r.enabled?'on':'off'}.`);});document.querySelectorAll('[data-delete]').forEach(b=>b.onclick=()=>{const r=S.schedule[Number(b.dataset.delete)];S.schedule.splice(Number(b.dataset.delete),1);S.editId='new';save(`${r.label} deleted from schedule.`);});document.querySelectorAll('[data-save-row]').forEach(b=>b.onclick=()=>saveRow(Number(b.dataset.saveRow)));document.querySelectorAll('[data-selann]').forEach(s=>{const r=S.schedule[Number(s.dataset.selann)];if(r?.announcementId)s.value=r.announcementId;});if($('playbackMode'))$('playbackMode').value=S.playbackMode;if($('autoStart'))$('autoStart').value=String(S.autoStart);if($('autoStop'))$('autoStop').value=String(S.autoStop);if($('saveHours'))$('saveHours').onclick=()=>{S.poolOpen=val('poolOpen');S.poolClose=val('poolClose');S.playbackMode=val('playbackMode')||'always';S.autoStart=val('autoStart')==='true';S.autoStop=val('autoStop')==='true';save('Playback automation saved.');};if($('verify'))$('verify').onclick=geocode;if($('gps'))$('gps').onclick=useGps;if($('checkWeather'))$('checkWeather').onclick=weather;if($('weatherAuto'))$('weatherAuto').value=String(S.weatherAuto);if($('saveLoc'))$('saveLoc').onclick=()=>{S.address=val('address');S.lat=val('lat');S.lon=val('lon');S.radius=val('radius');S.windGustMph=Math.max(15,Math.min(80,Number(val('windGustMph'))||35));S.weatherAuto=val('weatherAuto')!=='false';save('Weather settings saved.');};if($('voiceMode'))$('voiceMode').value=S.voiceMode;if($('aiVoice'))$('aiVoice').value=S.aiVoice;if($('deviceVoice'))$('deviceVoice').value=S.deviceVoice;if($('saveVoice'))$('saveVoice').onclick=()=>{S.voiceMode=val('voiceMode');S.aiVoice=val('aiVoice');S.deviceVoice=val('deviceVoice');S.rate=Number(val('rate'))||.94;S.pitch=Number(val('pitch'))||1;save('Voice settings saved.');};if($('testVoice'))$('testVoice').onclick=()=>{S.voiceMode=val('voiceMode');S.aiVoice=val('aiVoice');S.deviceVoice=val('deviceVoice');speak(`This is Serenity Shores Poolside Pulse version ${VERSION}. The selected voice is being tested.`,false);};if($('testDevice'))$('testDevice').onclick=()=>{const old=S.voiceMode;S.voiceMode='device';S.deviceVoice=val('deviceVoice');speak(`This is the device voice fallback test for Poolside Pulse version ${VERSION}.`,false).then(()=>{S.voiceMode=old;localSave();});};}
function saveRow(i){const r=S.schedule[i];if(!r){setFeedback('Schedule item not found.',false);return;}const kind=document.querySelector(`[data-kind="${i}"]`)?.value||'custom';const txt=document.querySelector(`[data-body="${i}"]`)?.value.trim()||'';const label=document.querySelector(`[data-label="${i}"]`)?.value||r.label;r.label=label;delete r.announcementId;delete r.url;delete r.text;delete r.trackIndex;if(kind==='song'){r.type='song';r.trackIndex=Math.max(0,Math.min(Number(document.querySelector(`[data-track="${i}"]`)?.value)||0,(S.tracks||[]).length-1));}else if(kind==='suno'||/^https?:\/\/(www\.)?suno\.com\//i.test(txt)){r.type='suno';r.url=txt;if(!r.url){setFeedback('Paste a Suno URL before saving.',false);return;}}else if(/^spotify:|^https?:\/\/open\.spotify\.com\//i.test(txt)){r.type='spotify';r.url=txt;}else if(kind==='custom'||txt){r.type='text';r.text=txt||'Type announcement text here.';}else{r.type='announcement';r.announcementId=document.querySelector(`[data-selann="${i}"]`)?.value||S.selected;}save(`Schedule item saved: ${r.label}.`);}

let spotifyPlayer=null;
let spotifyPlayerReady=false;
let spotifyWebDeviceId='';
let spotifyDucked=null;

function v5State(s){
  s={...(s||{})};
  s.activityLog=Array.isArray(s.activityLog)?s.activityLog.slice(0,120):[];
  s.voiceHealth=s.voiceHealth||'Not checked yet.';
  s.spotifyClientId=s.spotifyClientId||DEFAULT_SPOTIFY_CLIENT_ID;
  s.spotifyDeviceId=s.spotifyDeviceId||'';
  s.spotifyDeviceName=s.spotifyDeviceName||'';
  s.spotifyVolume=Number.isFinite(Number(s.spotifyVolume))?Math.max(20,Math.min(100,Number(s.spotifyVolume))):92;
  s.spotifyDuckedVolume=Number.isFinite(Number(s.spotifyDuckedVolume))?Math.max(0,Math.min(25,Number(s.spotifyDuckedVolume))):6;
  s.lightningRadiusMiles=Number.isFinite(Number(s.lightningRadiusMiles))?Math.max(1,Math.min(25,Number(s.lightningRadiusMiles))):10;
  s.lightningHoldMinutes=Number.isFinite(Number(s.lightningHoldMinutes))?Math.max(5,Math.min(90,Number(s.lightningHoldMinutes))):30;
  s.lightningClearAt=Number(s.lightningClearAt)||0;
  s.lightningLastStrikeKey=s.lightningLastStrikeKey||'';
  s.lightningLastStrikeAt=Number(s.lightningLastStrikeAt)||0;
  s.lightningAllClearSent=!!s.lightningAllClearSent;
  s.lightningText=s.lightningText||annText('lightning')||'Lightning has been detected within ten miles. Please exit the pool now. The pool must remain clear for 30 minutes, and another strike resets the clock.';
  s.windText=s.windText||annText('wind')||'Strong wind has been detected. Please close umbrellas and secure loose items.';
  s.lightningClearText=s.lightningClearText||annText('lightningclear')||'The lightning safety hold has ended. Lifeguards may reopen the pool when conditions remain safe.';
  return s;
}

cloudState=function(){const c={...clone(S),screen:'home',admin:false,tab:'music',editId:'new'};delete c.spotifyAccessToken;delete c.spotifyRefreshToken;delete c.spotifyTokenExpiresAt;if(S.screen!=='home'){delete c.intent;delete c.current;}return c;}
controlledProvider=function(){return S.musicProvider==='suno'||S.musicProvider==='spotify';}
function spotifyConfigured(){return !!String(S.spotifyClientId||'').trim();}
function spotifyToken(){try{return JSON.parse(localStorage.getItem(SPOTIFY_TOKEN_KEY)||'null')||null;}catch{return null;}}
function saveSpotifyToken(data){const prev=spotifyToken()||{};const next={...prev,...data,expiresAt:Date.now()+Math.max(60,Number(data.expires_in||3600)-60)*1000};if(!next.refresh_token&&prev.refresh_token)next.refresh_token=prev.refresh_token;localStorage.setItem(SPOTIFY_TOKEN_KEY,JSON.stringify(next));return next;}
function clearSpotifyToken(){localStorage.removeItem(SPOTIFY_TOKEN_KEY);setFeedback('Spotify login cleared on this device.',true);render();}
function spotifyLoggedIn(){const t=spotifyToken();return !!(t&&t.access_token);}
function spotifyRedirectUri(){return location.origin+location.pathname;}
function randString(len=64){const chars='abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789';let out='';const arr=new Uint8Array(len);crypto.getRandomValues(arr);arr.forEach(n=>out+=chars[n%chars.length]);return out;}
async function sha256base64url(input){const bytes=new TextEncoder().encode(input);const hash=await crypto.subtle.digest('SHA-256',bytes);return btoa(String.fromCharCode(...new Uint8Array(hash))).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'');}
async function spotifyLogin(){readMusicSettings();if(!spotifyConfigured()){setFeedback('Paste the Spotify Client ID first. Create it at developer.spotify.com/dashboard, then save music settings.',false);return;}const verifier=randString(96);const state=randString(24);sessionStorage.setItem('ppSpotifyVerifier',verifier);sessionStorage.setItem('ppSpotifyState',state);const url=new URL('https://accounts.spotify.com/authorize');url.searchParams.set('response_type','code');url.searchParams.set('client_id',S.spotifyClientId.trim());url.searchParams.set('scope','streaming user-read-email user-read-private user-read-playback-state user-modify-playback-state user-read-currently-playing');url.searchParams.set('redirect_uri',spotifyRedirectUri());url.searchParams.set('state',state);url.searchParams.set('code_challenge_method','S256');url.searchParams.set('code_challenge',await sha256base64url(verifier));location.href=url.toString();}
async function completeSpotifyLogin(){const params=new URLSearchParams(location.search);const code=params.get('code');if(!code)return;if(params.get('state')!==sessionStorage.getItem('ppSpotifyState')){setFeedback('Spotify login state did not match. Try Login again from the receiver.',false);return;}const verifier=sessionStorage.getItem('ppSpotifyVerifier');if(!verifier||!spotifyConfigured()){setFeedback('Spotify login cannot finish because the Client ID or verifier is missing.',false);return;}try{const body=new URLSearchParams({client_id:S.spotifyClientId.trim(),grant_type:'authorization_code',code,redirect_uri:spotifyRedirectUri(),code_verifier:verifier});const r=await fetch('https://accounts.spotify.com/api/token',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error_description||d.error||`Spotify token HTTP ${r.status}`);saveSpotifyToken(d);history.replaceState(null,'',location.pathname+location.hash);setFeedback('Spotify connected on this device. Tap Start Spotify Receiver to make this iPhone a Spotify Connect target.',true);}catch(e){setFeedback(`Spotify login failed: ${e.message}`,false);}}
async function spotifyAccessToken(){let t=spotifyToken();if(!t?.access_token)throw Error('Spotify is not connected on this receiver. Log in on the Home receiver first.');if(t.expiresAt>Date.now()+90000)return t.access_token;if(!t.refresh_token)throw Error('Spotify session expired. Log in again on the receiver.');const body=new URLSearchParams({client_id:S.spotifyClientId.trim(),grant_type:'refresh_token',refresh_token:t.refresh_token});const r=await fetch('https://accounts.spotify.com/api/token',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error_description||d.error||`Spotify refresh HTTP ${r.status}`);t=saveSpotifyToken(d);return t.access_token;}
async function spotifyApi(method,path,body=null,query={}){const token=await spotifyAccessToken();const url=new URL(`https://api.spotify.com/v1${path}`);Object.entries(query||{}).forEach(([k,v])=>{if(v!==undefined&&v!==null&&v!=='')url.searchParams.set(k,String(v));});const r=await fetch(url.toString(),{method,headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:body?JSON.stringify(body):undefined});if(r.status===204)return{};const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error?.message||d.error_description||`Spotify ${method} ${path} HTTP ${r.status}`);return d;}
function spotifyUri(input){const raw=String(input||'').trim();if(/^spotify:(playlist|album|artist|track):[A-Za-z0-9]+$/i.test(raw))return raw;try{const u=new URL(raw);if(!/open\.spotify\.com$/i.test(u.hostname))return raw;const parts=u.pathname.split('/').filter(Boolean);if(parts.length>=2&&['playlist','album','artist','track'].includes(parts[0]))return `spotify:${parts[0]}:${parts[1]}`;}catch{}return raw;}
function spotifyBody(input){const uri=spotifyUri(input||S.spotifyUrl);if(/^spotify:track:/i.test(uri))return{uris:[uri]};if(/^spotify:(playlist|album|artist):/i.test(uri))return{context_uri:uri};throw Error('Paste a Spotify playlist, album, artist, or track URL.');}
async function spotifyDevices(){const d=await spotifyApi('GET','/me/player/devices');return Array.isArray(d.devices)?d.devices:[];}
async function spotifyTargetDevice(){if(S.spotifyDeviceId)return S.spotifyDeviceId;if(spotifyWebDeviceId)return spotifyWebDeviceId;const devices=await spotifyDevices();const active=devices.find(d=>d.is_active);const named=devices.find(d=>/poolside pulse|serenity shores/i.test(d.name||''));const first=active||named||devices[0];if(first){S.spotifyDeviceId=first.id;S.spotifyDeviceName=first.name||'Spotify device';localSave();return first.id;}return'';}
async function ensureSpotifySdk(){if(window.Spotify)return window.Spotify;return await new Promise((resolve,reject)=>{const prior=window.onSpotifyWebPlaybackSDKReady;window.onSpotifyWebPlaybackSDKReady=()=>{prior&&prior();resolve(window.Spotify);};if(!document.querySelector('script[data-spotify-sdk]')){const s=document.createElement('script');s.src='https://sdk.scdn.co/spotify-player.js';s.async=true;s.dataset.spotifySdk='true';s.onerror=()=>reject(Error('Spotify SDK script failed to load.'));document.head.appendChild(s);}setTimeout(()=>window.Spotify?resolve(window.Spotify):reject(Error('Spotify SDK did not become ready.')),12000);});}
async function connectSpotifyReceiver(){readMusicSettings();if(!spotifyConfigured())throw Error('Spotify Client ID is required.');await spotifyAccessToken();const Spotify=await ensureSpotifySdk();if(spotifyPlayer&&spotifyPlayerReady)return spotifyWebDeviceId;spotifyPlayer=new Spotify.Player({name:'Poolside Pulse V5.5 Receiver',getOAuthToken:cb=>spotifyAccessToken().then(cb).catch(e=>{setFeedback(`Spotify token failed: ${e.message}`,false);}),volume:(Number(S.spotifyVolume)||92)/100});spotifyPlayer.addListener('ready',({device_id})=>{spotifyPlayerReady=true;spotifyWebDeviceId=device_id;S.spotifyDeviceId=device_id;S.spotifyDeviceName='Poolside Pulse V5.5 Receiver';localSave();setFeedback('Spotify receiver is ready on this device.',true);render();});spotifyPlayer.addListener('not_ready',()=>{spotifyPlayerReady=false;});spotifyPlayer.addListener('initialization_error',e=>setFeedback(`Spotify player init error: ${e.message}`,false));spotifyPlayer.addListener('authentication_error',e=>setFeedback(`Spotify auth error: ${e.message}`,false));spotifyPlayer.addListener('account_error',e=>setFeedback(`Spotify account error: ${e.message}. Premium is required.`,false));spotifyPlayer.addListener('playback_error',e=>setFeedback(`Spotify playback error: ${e.message}`,false));const ok=await spotifyPlayer.connect();if(!ok)throw Error('Spotify receiver did not connect.');return spotifyWebDeviceId;}
async function spotifySetVolume(percent){percent=Math.max(0,Math.min(100,Math.round(Number(percent)||0)));try{if(spotifyPlayer&&spotifyPlayerReady)await spotifyPlayer.setVolume(percent/100);}catch{}try{const device_id=await spotifyTargetDevice();await spotifyApi('PUT','/me/player/volume',null,{volume_percent:percent,device_id});}catch(e){if(percent>10)throw e;}}
commandOnly=function(push){return push&&S.screen!=='home';}
async function spotifyPlayUrl(url=S.spotifyUrl,push=true){receiverActive=true;readMusicSettings();if(!S.spotifyUrl&&url)S.spotifyUrl=url;const playUrl=url||S.spotifyUrl;if(commandOnly(push)){if(!playUrl){setFeedback('Paste a Spotify playlist or song URL first.',false);render();return;}S.musicProvider='spotify';S.spotifyUrl=playUrl;S.command={id:uid(),type:'spotify-play',url:playUrl,createdAt:Date.now()};await pushState('Spotify play command sent to Home receivers.');render();return;}if(!spotifyLoggedIn()){setFeedback('Spotify is not connected on this receiver. Use Login with Spotify on the Home receiver first.',false);render();return;}try{if(!S.spotifyDeviceId)await connectSpotifyReceiver().catch(()=>{});const device_id=await spotifyTargetDevice();if(!device_id)throw Error('No Spotify device found. Open Spotify on this iPhone or tap Start Spotify Receiver.');await spotifyApi('PUT','/me/player/play',spotifyBody(playUrl),{device_id});await spotifySetVolume(S.spotifyVolume);S.musicProvider='spotify';S.intent='playing';setFeedback('Spotify playback started on the receiver.',true);if(push){S.command={id:uid(),type:'spotify-play',url:playUrl,createdAt:Date.now()};await pushState('Spotify play command sent to Home receivers.');}}catch(e){setFeedback(`Spotify play failed: ${e.message}`,false);}render();}
async function spotifyPause(push=true){if(commandOnly(push)){S.musicProvider='spotify';S.intent='paused';S.command={id:uid(),type:'pause',createdAt:Date.now()};await pushState('Pause command sent to receivers.');render();return;}try{await spotifyApi('PUT','/me/player/pause',null,{device_id:await spotifyTargetDevice()});S.intent='paused';setFeedback('Spotify paused.',true);if(push){S.command={id:uid(),type:'pause',createdAt:Date.now()};await pushState('Pause command sent to receivers.');}}catch(e){setFeedback(`Spotify pause failed: ${e.message}`,false);}render();}
async function spotifyStop(push=true){if(commandOnly(push)){S.musicProvider='spotify';S.intent='stopped';S.command={id:uid(),type:'stop',createdAt:Date.now()};await pushState('Stop command sent to receivers.');render();return;}await spotifyPause(false);if(push){S.command={id:uid(),type:'stop',createdAt:Date.now()};await pushState('Stop command sent to receivers.');}}
async function spotifyNext(push=true){if(commandOnly(push)){S.musicProvider='spotify';S.intent='playing';S.command={id:uid(),type:'skip',createdAt:Date.now()};await pushState('Skip command sent to receivers.');render();return;}try{await spotifyApi('POST','/me/player/next',null,{device_id:await spotifyTargetDevice()});S.intent='playing';setFeedback('Spotify skipped to next track.',true);if(push){S.command={id:uid(),type:'skip',createdAt:Date.now()};await pushState('Skip command sent to receivers.');}}catch(e){setFeedback(`Spotify skip failed: ${e.message}`,false);}render();}
async function duckSpotifyForAnnouncement(hold=false){if(S.musicProvider!=='spotify'||S.intent!=='playing')return false;spotifyDucked={volume:S.spotifyVolume,hold};try{if(hold){await spotifySetVolume(S.spotifyDuckedVolume);await spotifyApi('PUT','/me/player/pause',null,{device_id:await spotifyTargetDevice()});S.intent='paused';}else{await spotifySetVolume(S.spotifyDuckedVolume);}setFeedback(hold?'Spotify paused for safety announcement.':'Spotify volume lowered for announcement.',true);return true;}catch(e){setFeedback(`Spotify ducking failed: ${e.message}`,false);return false;}}
async function restoreSpotifyAfterAnnouncement(){if(!spotifyDucked)return;const snap=spotifyDucked;spotifyDucked=null;if(snap.hold){setFeedback('Safety hold active; Spotify remains paused.',true);return;}try{await spotifyApi('PUT','/me/player/play',null,{device_id:await spotifyTargetDevice()});await spotifySetVolume(snap.volume||S.spotifyVolume);S.intent='playing';setFeedback('Spotify restored after announcement.',true);}catch(e){setFeedback(`Spotify restore failed: ${e.message}`,false);}}
providerNotice=function(){if(S.musicProvider==='spotify')return 'Spotify is controllable through the official Spotify login and Spotify Connect. Use a Premium account on the receiver, paste playlist or track URLs, and commands can play, pause, skip, and lower volume before announcements.';return 'Suno/direct audio is controlled by Poolside Pulse, so music can fade down before announcements.';}
openProvider=async function(push=false){readMusicSettings();if(S.musicProvider==='spotify'){await spotifyPlayUrl(S.spotifyUrl,push);return;}const url=activeProviderUrl();if(!url){setFeedback(`Paste a ${providerLabel()} playlist URL first.`,false);return;}window.open(url,'_blank','noopener,noreferrer');S.intent='external';setFeedback(`${providerLabel()} opened. Keep this iPhone volume high; use Poolside Pulse for announcements and weather alerts.`,true);if(push){S.command={id:uid(),type:'provider',provider:S.musicProvider,url,createdAt:Date.now()};pushState(`${providerLabel()} player opened on Home receivers.`);}render();}
play=async function(push=true){receiverActive=true;if(S.musicProvider==='spotify'){await spotifyPlayUrl(S.spotifyUrl,push);return;}if(!controlledProvider()){openProvider(push);return;}if(!hasPlayable()){await importSuno('Playlist auto-imported for receiver.');if(!hasPlayable()){S.intent='stopped';setFeedback('No playable Suno/direct audio loaded. Import the playlist first.',false);if(push)await pushState('Play command sent, but no playable audio is loaded.');render();return;}}if(!track().audioUrl){const n=nextPlayableIndex(S.current-1);if(n>=0)S.current=n;}const url=track().audioUrl;if(!sameUrl(music.src,url))music.src=url;try{music.volume=Math.max(music.volume||.02,.02);music.muted=false;await music.play();await fade(music,.95,650);S.intent='playing';setFeedback(`Receiver playing: ${track().title}`,true);}catch(e){S.intent='stopped';setFeedback(`Browser blocked audio. Click Activate Sound / Play Station on this Home receiver. ${e.message||''}`,false);}if(push){S.command={id:uid(),type:'play',createdAt:Date.now()};await pushState('Play command sent to all Home receivers.');}render();}
pause=async function(push=true){if(S.musicProvider==='spotify'){await spotifyPause(push);return;}await fade(music,0,300);music.pause();music.volume=.95;S.intent='paused';setFeedback('Paused.',true);if(push){S.command={id:uid(),type:'pause'};await pushState('Pause command sent to receivers.');}render();}
stop=async function(push=true){if(S.musicProvider==='spotify'){await spotifyStop(push);return;}await fade(music,0,300);music.pause();music.currentTime=0;music.volume=.95;S.intent='stopped';setFeedback('Stopped.',true);if(push){S.command={id:uid(),type:'stop'};await pushState('Stop command sent to receivers.');}render();}
nextTrack=async function(push=true,message='Skipped to next playable track.'){if(S.musicProvider==='spotify'){await spotifyNext(push);return;}if(!controlledProvider()){setFeedback(`Use the ${providerLabel()} player to skip tracks.`,false);return;}const n=nextPlayableIndex(S.current);if(n<0){S.intent='stopped';setFeedback('No next playable track found.',false);if(push)await pushState('Skip command sent, but no next playable track exists.');render();return;}S.current=n;const url=track().audioUrl;if(!sameUrl(music.src,url))music.src=url;if(S.intent==='playing'||receiverActive){try{music.muted=false;music.volume=Math.max(music.volume||.02,.02);await music.play();S.intent='playing';setFeedback(message,true);}catch(e){setFeedback(`Next track blocked. ${e.message||'Tap Play on receiver.'}`,false);}}if(push){S.command={id:uid(),type:'skip'};await pushState('Skip command sent to receivers.');}render();}
itemBody=function(item){if(item.type==='suno')return item.url||'';if(item.type==='spotify')return `Spotify: ${item.url||''}`;if(item.type==='song'){const t=S.tracks?.[Number(item.trackIndex)||0];return t?`Specific song: ${t.title}`:'Specific song from current queue';}return item.announcementId?annText(item.announcementId):(item.text||'');}
playScheduleItem=async function(item){if(!item){setFeedback('No schedule item selected.',false);return;}if(item.type==='spotify'){if(!item.url){setFeedback('This schedule item has no Spotify URL.',false);return;}S.musicProvider='spotify';S.spotifyUrl=item.url;S.command={id:uid(),type:'spotify-play',url:item.url,label:item.label,createdAt:Date.now()};await pushState(`Spotify item sent to receivers: ${item.label}`);if(S.screen==='home')await spotifyPlayUrl(item.url,false);render();return;}if(item.type==='song'){const index=Math.max(0,Math.min(Number(item.trackIndex)||0,(S.tracks||[]).length-1));S.current=index;S.command={id:uid(),type:'song',trackIndex:index,label:item.label,createdAt:Date.now()};await pushState(`Specific song sent to receivers: ${track().title}`);if(S.screen==='home')await play(false);render();return;}if(item.type==='suno'){if(!item.url){setFeedback('This schedule item has no Suno URL.',false);return;}S.command={id:uid(),type:'suno',url:item.url,label:item.label,createdAt:Date.now()};await pushState(`Suno item sent to receivers: ${item.label}`);if(S.screen==='home'){S.musicProvider='suno';S.playlistUrl=item.url;await importSuno('Scheduled Suno URL imported.');await play(false);}render();}else{await sendAnnouncement(itemBody(item),!!item.hold);}}
applyRemote=async function(newCommand,newAnnouncement){if(S.screen!=='home')return;if(newCommand&&S.command?.id&&S.command.id!==lastCommandId&&!speaking){lastCommandId=S.command.id;if(S.command.type==='play')await play(false);if(S.command.type==='pause')await pause(false);if(S.command.type==='stop')await stop(false);if(S.command.type==='skip')await nextTrack(false);if(S.command.type==='spotify-play'){S.musicProvider='spotify';S.spotifyUrl=S.command.url||S.spotifyUrl;await spotifyPlayUrl(S.spotifyUrl,false);}if(S.command.type==='provider'){S.musicProvider=S.command.provider||S.musicProvider;if(S.command.provider==='spotify')S.spotifyUrl=S.command.url||S.spotifyUrl;openProvider(false);}if(S.command.type==='song'){S.current=Math.max(0,Math.min(Number(S.command.trackIndex)||0,(S.tracks||[]).length-1));await play(false);}if(S.command.type==='suno'){S.musicProvider='suno';S.playlistUrl=S.command.url;await importSuno('Remote Suno URL imported.');await play(false);}}if(newAnnouncement&&S.announcement?.id&&S.announcement.id!==lastAnnouncementId){lastAnnouncementId=S.announcement.id;await speak(S.announcement.text,S.announcement.hold);}}
speak=async function(text,hold=false){const msg=tokens(text).trim();if(!msg){setFeedback('Announcement text is empty.',false);return;}if(speaking){queue.push({text:msg,hold});setFeedback('Announcement queued behind current announcement.',true);return;}speaking=true;const hadMusic=musicAudible();const hadSpotify=await duckSpotifyForAnnouncement(hold);if(hadMusic){setFeedback('Announcement starting; music fading to zero without restarting track.',true);await fade(music,0,900);try{music.pause();}catch{}}else if(!hadSpotify&&!controlledProvider()&&S.intent==='external'){setFeedback(`${providerLabel()} is outside app volume control. Announcement will play at full app volume.`,true);}let spoken=false;const finish=async()=>{speaking=false;if(hadSpotify&&!hold&&shouldResumeMusic()){await restoreSpotifyAfterAnnouncement();}else if(hadSpotify&&hold){spotifyDucked=null;S.intent='paused';setFeedback('Safety hold active; Spotify remains paused.',true);}else if(hadMusic&&!hold&&shouldResumeMusic()){try{music.muted=false;music.volume=Math.max(music.volume||.02,.02);await music.play();await fade(music,.95,1050);S.intent='playing';setFeedback('Announcement finished; same track faded back up without restart.',true);}catch(e){setFeedback(`Could not fade music back up: ${e.message}`,false);}}else if(hold&&hadMusic){music.pause();S.intent='paused';setFeedback('Safety hold finished; music remains paused.',true);}localSave();render();const next=queue.shift();if(next)setTimeout(()=>speak(next.text,next.hold),250);};
if(S.voiceMode==='ai'){try{const r=await fetch(API.tts,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text:msg,voice:S.aiVoice||'marin',instructions:'Speak like a calm, polished, natural resort public-address announcer at Serenity Shores. Be clear, warm, and firm for safety.'})});if(r.ok){const blob=await r.blob();const url=URL.createObjectURL(blob);const a=new Audio(url);a.preload='auto';a.playsInline=true;a.muted=false;a.volume=1;a.onended=()=>{URL.revokeObjectURL(url);finish();};a.onerror=()=>{URL.revokeObjectURL(url);setFeedback('AI announcement audio failed during playback.',false);finish();};await a.play();spoken=true;setFeedback(`AI voice playing at full app volume: ${S.aiVoice}.`,true);}else{const d=await r.json().catch(()=>({}));setFeedback(`AI voice failed ${r.status}: ${d.error||d.detail||'check OpenAI key/billing/limits'}. Device fallback used.`,false);}}catch(e){setFeedback(`AI voice error: ${e.message}. Device fallback used.`,false);}}
if(!spoken){if('speechSynthesis'in window){speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(msg);const v=voices.find(v=>v.name===S.deviceVoice)||bestVoices()[0];if(v)u.voice=v;u.rate=Number(S.rate)||.94;u.pitch=Number(S.pitch)||1;u.volume=1;u.onend=finish;u.onerror=finish;speechSynthesis.speak(u);setFeedback(`Device voice speaking at full app volume${v?': '+v.name:''}.`,true);}else{setFeedback('No speech engine available in this browser.',false);await finish();}}}
function lightningRemainingText(){const left=Math.max(0,Number(S.lightningClearAt||0)-Date.now());if(!left)return'No active lightning hold.';const mins=Math.ceil(left/60000);return `${mins} minute${mins===1?'':'s'} remaining in lightning hold.`;}
function lightningThreat(d){return /lightning|thunder/i.test(d.threatType||'');}
function lightningStrikeKey(d){const hit=d.lightningHits?.[0]||d.thunderHits?.[0]||{};return hit.id||hit.dateTimeISO||`${d.threatType}:${String(d.summary||'').slice(0,120)}`;}
weatherAnnouncement=function(d){if(/wind/i.test(d.threatType||''))return tokens(S.windText);if(/tornado/i.test(d.threatType||''))return'Attention guests and lifeguards. A tornado warning has been detected near Serenity Shores. Exit the pool area and move indoors immediately.';const hit=d.lightningHits?.[0];const dist=hit?.distanceMI?` The closest detected strike was about ${Math.round(hit.distanceMI*10)/10} miles away.`:'';const reset=S.lightningClearAt>Date.now()?' Another strike has been detected, so the 30 minute safety clock has reset.':'';return `${tokens(S.lightningText)}${dist}${reset}`;}
weather=async function(){const lat=parseCoord(S.lat),lon=parseCoord(S.lon),radius=Math.max(1,Math.min(25,Number(S.radius)||10)),windGustMph=Math.max(15,Math.min(80,Number(S.windGustMph)||35)),lightningRadius=Math.max(1,Math.min(25,Number(S.lightningRadiusMiles)||10));if(lat===null||lon===null){S.weather='Enter valid latitude and longitude, or tap Use Device GPS.';S.weatherCheckedAt=stamp();setFeedback('Weather check failed: invalid coordinates.',false);await save('Weather settings saved, but coordinates are invalid.');return;}try{const params=new URLSearchParams({lat:String(lat),lon:String(lon),radiusMiles:String(radius),windGustMph:String(windGustMph),lightningRadiusMiles:String(lightningRadius)});const r=await fetch(`${API.weather}?${params.toString()}`);const d=await r.json().catch(()=>({}));if(!r.ok||d.ok===false)throw Error(d.error||`Weather HTTP ${r.status}`);S.weather=d.summary||'Weather checked.';S.weatherCheckedAt=stamp();S.lat=String(lat);S.lon=String(lon);S.radius=radius;S.windGustMph=windGustMph;S.lightningRadiusMiles=lightningRadius;if(d.threat&&lightningThreat(d)){const strikeKey=lightningStrikeKey(d);if(S.lightningLastStrikeKey!==strikeKey){S.lightningLastStrikeKey=strikeKey;S.lightningLastStrikeAt=Date.now();S.lightningClearAt=Date.now()+Math.max(5,Number(S.lightningHoldMinutes)||30)*60000;S.lightningAllClearSent=false;S.weatherLastThreatKey=`${today()}:lightning:${strikeKey}`;await sendAnnouncement(weatherAnnouncement(d),true);}else{setFeedback(`Lightning threat still present; ${lightningRemainingText()}`,true);}}else if(d.threat){const hold=/tornado/i.test(d.threatType||'');const threatKey=`${today()}:${d.threatType}:${String(d.summary||'').slice(0,120)}`;if(S.weatherLastThreatKey!==threatKey){S.weatherLastThreatKey=threatKey;await sendAnnouncement(weatherAnnouncement(d),hold);}else{setFeedback('Weather threat still present; duplicate announcement suppressed for this matching alert.',true);}}else if(S.lightningClearAt&&Date.now()>=S.lightningClearAt&&!S.lightningAllClearSent){S.lightningAllClearSent=true;S.lightningClearAt=0;await sendAnnouncement(tokens(S.lightningClearText),false);}await save(`Weather checked and saved. ${S.lightningClearAt>Date.now()?lightningRemainingText():''}`);}catch(e){S.weather=`Weather check failed: ${e.message}`;S.weatherCheckedAt=stamp();setFeedback(`Weather check failed: ${e.message}`,false);await save('Weather failure saved for screenshot troubleshooting.');}}
tick=async function(){if(S.screen==='home'&&receiverActive&&!speaking){if(S.autoStart&&shouldPlayContinuously()&&S.intent!=='playing'&&!(S.lightningClearAt>Date.now()))await play(false);if(S.autoStop&&S.playbackMode==='hours'&&!openNow()&&S.intent==='playing')await stop(false);const now=hm(new Date().getHours()*60+new Date().getMinutes());for(const it of S.schedule){if(!it.enabled||schedTime(it)!==now)continue;const key=today()+':'+it.id+':'+schedTime(it);if(S.lastRun[it.id]===key)continue;S.lastRun[it.id]=key;localSave();await playScheduleItem(it);}}}
musicPage=function(){const q=S.tracks.map((x,i)=>`<div class="q ${i===S.current?'cur':''}"><div><b>${i+1}. ${esc(x.title)}</b><span>${esc(x.artist)} · ${x.audioUrl?'playable':'title only'}</span></div><button data-song="${i}">Play</button></div>`).join('');const providers=Object.entries(PROVIDERS).map(([id,label])=>`<option value="${id}" ${S.musicProvider===id?'selected':''}>${label}</option>`).join('');const spotifyStatus=spotifyLoggedIn()?'Connected on this device':'Not connected on this device';return shell(`<section class="panel"><h1>Music Source</h1><p>Provider: ${esc(providerLabel())}. ${esc(providerNotice())}</p><div class="grid2"><label>Music Provider<select id="musicProvider">${providers}</select></label><label>Playlist Name<input id="playlistName" value="${esc(S.playlistName)}"></label></div><label>Suno Playlist URL<input id="playlistUrl" value="${esc(S.playlistUrl)}"></label><label>Spotify Playlist or Song URL<input id="spotifyUrl" value="${esc(S.spotifyUrl)}" placeholder="https://open.spotify.com/playlist/..."></label><div class="grid2"><label>Spotify Client ID<input id="spotifyClientId" value="${esc(S.spotifyClientId)}" placeholder="From developer.spotify.com dashboard"></label><label>Spotify Receiver Device<input id="spotifyDeviceName" value="${esc(S.spotifyDeviceName||S.spotifyDeviceId||'Auto')}" readonly></label></div><div class="grid2"><label>Spotify Music Volume<input id="spotifyVolume" inputmode="numeric" value="${esc(S.spotifyVolume)}"></label><label>Announcement Duck Volume<input id="spotifyDuckedVolume" inputmode="numeric" value="${esc(S.spotifyDuckedVolume)}"></label></div><div class="status"><b>Spotify:</b> ${esc(spotifyStatus)} · Redirect URI: <code>${esc(spotifyRedirectUri())}</code></div><div class="actions"><button id="saveMusic">Save Music Settings</button><button id="spotifyLogin" class="secondary">Login with Spotify</button><button id="spotifyReceiver" class="secondary">Start Spotify Receiver</button><button id="spotifyClear" class="secondary">Clear Spotify Login</button><button id="import" class="secondary">Import Suno Queue</button><button id="openProvider" class="secondary">Open/Play Provider</button><button id="playCmd" class="secondary">Play Receivers</button><button id="pauseCmd" class="secondary">Pause</button><button id="skipCmd" class="secondary">Skip</button><button id="stopCmd" class="secondary">Stop</button></div></section><section class="panel"><h2>Published Queue</h2>${q}</section>`);}
weatherPage=function(){return shell(`<section class="panel"><h1>Location & Weather</h1><label>Address<input id="address" value="${esc(S.address)}"></label><div class="actions"><button id="verify">Verify Address</button><button id="gps" class="secondary">Use Device GPS</button><button id="checkWeather" class="secondary">Check Weather</button></div><div class="grid3"><label>Latitude<input id="lat" value="${esc(S.lat)}"></label><label>Longitude<input id="lon" value="${esc(S.lon)}"></label><label>Weather Radius Miles<input id="radius" value="${esc(S.radius)}"></label></div><div class="grid3"><label>Lightning Radius Miles<input id="lightningRadiusMiles" value="${esc(S.lightningRadiusMiles)}"></label><label>Lightning Hold Minutes<input id="lightningHoldMinutes" value="${esc(S.lightningHoldMinutes)}"></label><label>Strong Wind Alert MPH<input id="windGustMph" inputmode="numeric" value="${esc(S.windGustMph)}"></label></div><label>Auto Scan<select id="weatherAuto"><option value="true">Every 5 minutes on Home receiver</option><option value="false">Manual only</option></select></label><label>Lightning Announcement<textarea id="lightningText">${esc(S.lightningText)}</textarea></label><label>Strong Wind Announcement<textarea id="windText">${esc(S.windText)}</textarea></label><label>Lightning All Clear Announcement<textarea id="lightningClearText">${esc(S.lightningClearText)}</textarea></label><button id="saveLoc">Save Weather Settings</button><div class="status"><b>Weather:</b> ${esc(S.weather)}<br><b>${esc(S.weatherCheckedAt?'Last checked '+S.weatherCheckedAt:'Not checked yet')}</b><br><b>Lightning:</b> ${esc(lightningRemainingText())}</div></section>`);}
bind=function(){if($('home'))$('home').onclick=()=>{S.screen='home';localSave();render();};if($('cmd'))$('cmd').onclick=()=>{S.screen='command';localSave();render();};if($('login'))$('login').onclick=()=>{if(val('pin')===PIN){S.admin=true;S.screen='command';S.tab='music';setFeedback('Admin unlocked.',true);render();}else setFeedback('Wrong PIN.',false);};document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{S.tab=b.dataset.tab;localSave();render();});if($('playHome'))$('playHome').onclick=()=>S.intent==='playing'?pause(true):play(true);if($('skipHome'))$('skipHome').onclick=()=>nextTrack(true);if($('stopHome'))$('stopHome').onclick=()=>stop(true);if($('musicProvider'))$('musicProvider').value=S.musicProvider;if($('saveMusic'))$('saveMusic').onclick=()=>{readMusicSettings();S.spotifyClientId=val('spotifyClientId')||S.spotifyClientId;S.spotifyVolume=Math.max(20,Math.min(100,Number(val('spotifyVolume'))||92));S.spotifyDuckedVolume=Math.max(0,Math.min(25,Number(val('spotifyDuckedVolume'))||6));save('Music provider settings saved.');};if($('spotifyLogin'))$('spotifyLogin').onclick=spotifyLogin;if($('spotifyReceiver'))$('spotifyReceiver').onclick=()=>connectSpotifyReceiver().then(()=>setFeedback('Spotify receiver started. Now press Play Receivers.',true)).catch(e=>setFeedback(`Spotify receiver failed: ${e.message}`,false));if($('spotifyClear'))$('spotifyClear').onclick=clearSpotifyToken;if($('import'))$('import').onclick=()=>{S.musicProvider='suno';importSuno();};if($('openProvider'))$('openProvider').onclick=()=>{readMusicSettings();openProvider(true);};if($('playCmd'))$('playCmd').onclick=()=>{readMusicSettings();play(true);};if($('pauseCmd'))$('pauseCmd').onclick=()=>pause(true);if($('skipCmd'))$('skipCmd').onclick=()=>nextTrack(true);if($('stopCmd'))$('stopCmd').onclick=()=>stop(true);document.querySelectorAll('[data-song]').forEach(b=>b.onclick=()=>{S.current=Number(b.dataset.song);S.musicProvider='suno';play(true);});document.querySelectorAll('[data-ann]').forEach(b=>b.onclick=()=>{const a=S.anns.find(x=>x.id===b.dataset.ann);if(a){S.selected=a.id;S.quickText=a.text;setFeedback(`Loaded template: ${a.label}`,true);localSave();render();}});if($('newAnn'))$('newAnn').onclick=()=>{const id='custom-'+uid();S.anns.push({id,label:'New Template',text:'Type announcement text here.'});S.selected=id;S.quickText='Type announcement text here.';save('New announcement template added.');};if($('saveAnn'))$('saveAnn').onclick=()=>{const a=ann();a.label=val('annLabel')||a.label;a.text=val('annText')||a.text;S.guestName=val('guestName');save('Announcement template saved.');};if($('deleteAnn'))$('deleteAnn').onclick=()=>{if(S.anns.length<2){setFeedback('At least one template must remain.',false);return;}S.anns=S.anns.filter(a=>a.id!==S.selected);S.selected=S.anns[0].id;save('Announcement template deleted.');};if($('quickPlay'))$('quickPlay').onclick=()=>{S.quickText=val('quickText');sendAnnouncement(S.quickText,false);};if($('quickHold'))$('quickHold').onclick=()=>{S.quickText=val('quickText');sendAnnouncement(S.quickText,true);};if($('quickSchedule'))$('quickSchedule').onclick=()=>{S.quickText=val('quickText');const it={id:'sched-'+uid(),label:'Custom Announcement',type:'text',time:'10:00',text:S.quickText,enabled:true};S.schedule.push(it);S.editId=it.id;save('Quick text added to daily schedule.');};if($('addSched'))$('addSched').onclick=()=>{const it={id:'sched-'+uid(),label:'New Schedule Item',type:'text',time:'10:00',text:'Type announcement text here.',enabled:true};S.schedule.push(it);S.editId=it.id;save('New schedule item added.');};document.querySelectorAll('[data-time]').forEach(input=>input.onchange=()=>{const r=S.schedule[Number(input.dataset.time)];delete r.offsetToClose;r.time=input.value;save(`Time updated for ${r.label}.`);});document.querySelectorAll('[data-play-row]').forEach(b=>b.onclick=()=>playScheduleItem(S.schedule[Number(b.dataset.playRow)]));document.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>{const r=S.schedule[Number(b.dataset.edit)];S.editId=S.editId===r.id?'new':r.id;setFeedback(S.editId==='new'?'Edit closed.':`Editing ${r.label}.`,true);localSave();render();});document.querySelectorAll('[data-toggle]').forEach(b=>b.onclick=()=>{const r=S.schedule[Number(b.dataset.toggle)];r.enabled=!r.enabled;save(`${r.label} turned ${r.enabled?'on':'off'}.`);});document.querySelectorAll('[data-delete]').forEach(b=>b.onclick=()=>{const r=S.schedule[Number(b.dataset.delete)];S.schedule.splice(Number(b.dataset.delete),1);S.editId='new';save(`${r.label} deleted from schedule.`);});document.querySelectorAll('[data-save-row]').forEach(b=>b.onclick=()=>saveRow(Number(b.dataset.saveRow)));document.querySelectorAll('[data-selann]').forEach(s=>{const r=S.schedule[Number(s.dataset.selann)];if(r?.announcementId)s.value=r.announcementId;});if($('playbackMode'))$('playbackMode').value=S.playbackMode;if($('autoStart'))$('autoStart').value=String(S.autoStart);if($('autoStop'))$('autoStop').value=String(S.autoStop);if($('saveHours'))$('saveHours').onclick=()=>{S.poolOpen=val('poolOpen');S.poolClose=val('poolClose');S.playbackMode=val('playbackMode')||'always';S.autoStart=val('autoStart')==='true';S.autoStop=val('autoStop')==='true';save('Playback automation saved.');};if($('verify'))$('verify').onclick=geocode;if($('gps'))$('gps').onclick=useGps;if($('checkWeather'))$('checkWeather').onclick=weather;if($('weatherAuto'))$('weatherAuto').value=String(S.weatherAuto);if($('saveLoc'))$('saveLoc').onclick=()=>{S.address=val('address');S.lat=val('lat');S.lon=val('lon');S.radius=val('radius');S.windGustMph=Math.max(15,Math.min(80,Number(val('windGustMph'))||35));S.lightningRadiusMiles=Math.max(1,Math.min(25,Number(val('lightningRadiusMiles'))||10));S.lightningHoldMinutes=Math.max(5,Math.min(90,Number(val('lightningHoldMinutes'))||30));S.lightningText=val('lightningText')||S.lightningText;S.windText=val('windText')||S.windText;S.lightningClearText=val('lightningClearText')||S.lightningClearText;S.weatherAuto=val('weatherAuto')!=='false';save('Weather settings saved.');};if($('voiceMode'))$('voiceMode').value=S.voiceMode;if($('aiVoice'))$('aiVoice').value=S.aiVoice;if($('deviceVoice'))$('deviceVoice').value=S.deviceVoice;if($('saveVoice'))$('saveVoice').onclick=()=>{S.voiceMode=val('voiceMode');S.aiVoice=val('aiVoice');S.deviceVoice=val('deviceVoice');S.rate=Number(val('rate'))||.94;S.pitch=Number(val('pitch'))||1;save('Voice settings saved.');};if($('testVoice'))$('testVoice').onclick=()=>{S.voiceMode=val('voiceMode');S.aiVoice=val('aiVoice');S.deviceVoice=val('deviceVoice');speak(`This is Serenity Shores Poolside Pulse version ${VERSION}. The selected voice is being tested.`,false);};if($('testDevice'))$('testDevice').onclick=()=>{const old=S.voiceMode;S.voiceMode='device';S.deviceVoice=val('deviceVoice');speak(`This is the device voice fallback test for Poolside Pulse version ${VERSION}.`,false).then(()=>{S.voiceMode=old;localSave();});};}
activeProviderUrl=function(){return S.musicProvider==='spotify'?S.spotifyUrl:S.playlistUrl;}
readMusicSettings=function(){S.musicProvider=val('musicProvider')||S.musicProvider;if(!PROVIDERS[S.musicProvider])S.musicProvider='suno';S.playlistName=val('playlistName')||S.playlistName;S.playlistUrl=val('playlistUrl')||S.playlistUrl;S.spotifyUrl=val('spotifyUrl')||S.spotifyUrl;localSave();}
providerNotice=function(){return S.musicProvider==='spotify'?'Spotify uses the official Spotify login on the receiver. Command phones send play, pause, skip, stop, and volume-safe announcement commands to the logged-in receiver.':'Suno runs inside Poolside Pulse after import, so receivers can schedule specific songs, fade music down, and resume after announcements.';}
cloudState=function(){const c={...clone(S),screen:'home',admin:false,tab:'command',editId:'new'};delete c.spotifyAccessToken;delete c.spotifyRefreshToken;delete c.spotifyTokenExpiresAt;if(S.screen!=='home'){delete c.intent;delete c.current;}return c;}
function compactUrl(url){return String(url||'').replace(/^https?:\/\//,'').slice(0,70);}
function logRows(limit=60){return(Array.isArray(S.activityLog)?S.activityLog:[]).slice(0,limit).map(e=>`<div class="logRow ${esc(e.kind||'')}"><div><b>${esc(e.title||e.kind||'Event')}</b><span>${esc(e.detail||'')}</span></div><small>${esc(e.time||'')}<br>${esc(e.device||'')}</small></div>`).join('')||'<p class="muted">No receiver activity has been logged yet.</p>';}
function currentStatusCards(){return `<div class="statusGrid"><div><b>Provider</b><strong>${esc(providerLabel())}</strong></div><div><b>Station</b><strong>${esc(S.intent||'stopped')}</strong></div><div><b>Weather</b><strong>${esc(S.weatherCheckedAt?'checked':'standing by')}</strong></div><div><b>Voice</b><strong>${esc(S.voiceMode==='ai'?'AI first':'device')}</strong></div></div>`;}
function commandOnly(push){return push&&S.screen!=='home';}
async function triggerWeatherCheck(){if(S.screen!=='home'){await issueCommand('weather-check',{label:'Weather check',detail:'Manual weather check requested from command.'},'Weather check command sent to receivers.');return;}await weather();}
async function checkVoiceHealth(){try{const r=await fetch(API.voiceHealth,{cache:'no-store'});const d=await r.json().catch(()=>({}));if(!r.ok||d.ok===false)throw Error(d.error||`Voice health HTTP ${r.status}`);S.voiceHealth=d.note||'Voice health checked.';setFeedback(S.voiceHealth,true);logEvent('voice','Voice health check',S.voiceHealth);}catch(e){S.voiceHealth=`Voice health check failed: ${e.message}`;setFeedback(S.voiceHealth,false);}await save('Voice health saved.');}
openProvider=async function(push=false){readMusicSettings();if(S.musicProvider==='spotify'){await spotifyPlayUrl(S.spotifyUrl,push);return;}await play(push);}
play=async function(push=true){receiverActive=true;readMusicSettings();if(commandOnly(push)){const payload={label:S.musicProvider==='spotify'?'Play Spotify':'Play Suno',provider:S.musicProvider,url:activeProviderUrl(),trackIndex:S.current,detail:S.musicProvider==='spotify'?compactUrl(S.spotifyUrl):`Queue track ${S.current+1}`};if(S.musicProvider==='spotify')await issueCommand('spotify-play',{...payload,url:S.spotifyUrl},'Spotify play command sent to receivers.');else await issueCommand('play',payload,'Suno play command sent to receivers.');return;}if(S.musicProvider==='spotify'){await spotifyPlayUrl(S.spotifyUrl,push);return;}S.musicProvider='suno';if(!hasPlayable()){await importSuno('Playlist auto-imported for receiver.');if(!hasPlayable()){S.intent='stopped';setFeedback('No playable Suno audio loaded. Import the Suno queue first.',false);if(push)await issueCommand('play',{label:'Play Suno failed',provider:'suno'},'Play command sent, but no playable audio is loaded.');render();return;}}if(!track().audioUrl){const n=nextPlayableIndex(S.current-1);if(n>=0)S.current=n;}const url=track().audioUrl;if(!sameUrl(music.src,url))music.src=url;try{music.volume=Math.max(music.volume||.02,.02);music.muted=false;await music.play();await fade(music,.95,650);S.intent='playing';logEvent('play','Suno playing',`${track().title} - ${track().artist}`,{provider:'suno',trackIndex:S.current});setFeedback(`Receiver playing: ${track().title}`,true);if(push){S.command={id:uid(),type:'play',provider:'suno',trackIndex:S.current,createdAt:Date.now(),source:deviceLabel()};await pushState('Play command sent to all Home receivers.');}else if(S.screen==='home')await pushState('Receiver play logged.');}catch(e){S.intent='stopped';setFeedback(`Browser blocked audio. Tap Activate Sound / Play Station on this receiver. ${e.message||''}`,false);}render();}
pause=async function(push=true){if(commandOnly(push)){await issueCommand('pause',{label:'Pause all receivers'},'Pause command sent to receivers.');return;}if(S.musicProvider==='spotify'){await spotifyPause(push);return;}await fade(music,0,300);music.pause();music.volume=.95;S.intent='paused';logEvent('pause','Paused',track().title||providerLabel());setFeedback('Paused receivers.',true);if(push){S.command={id:uid(),type:'pause',createdAt:Date.now(),source:deviceLabel()};await pushState('Pause command sent to receivers.');}else if(S.screen==='home')await pushState('Receiver pause logged.');render();}
stop=async function(push=true){if(commandOnly(push)){await issueCommand('stop',{label:'Stop all receivers'},'Stop command sent to receivers.');return;}if(S.musicProvider==='spotify'){await spotifyStop(push);logEvent('stop','Spotify stopped','');return;}await fade(music,0,300);music.pause();music.currentTime=0;music.volume=.95;S.intent='stopped';logEvent('stop','Stopped',track().title||providerLabel());setFeedback('Stopped receivers.',true);if(push){S.command={id:uid(),type:'stop',createdAt:Date.now(),source:deviceLabel()};await pushState('Stop command sent to receivers.');}else if(S.screen==='home')await pushState('Receiver stop logged.');render();}
nextTrack=async function(push=true,message='Skipped to next playable track.'){if(commandOnly(push)){await issueCommand('skip',{label:'Skip track'},'Skip command sent to receivers.');return;}if(S.musicProvider==='spotify'){await spotifyNext(push);logEvent('skip','Spotify skipped','');return;}S.musicProvider='suno';const n=nextPlayableIndex(S.current);if(n<0){S.intent='stopped';setFeedback('No next playable track found.',false);render();return;}S.current=n;const url=track().audioUrl;if(!sameUrl(music.src,url))music.src=url;if(S.intent==='playing'||receiverActive){try{music.muted=false;music.volume=Math.max(music.volume||.02,.02);await music.play();S.intent='playing';logEvent('skip','Suno skipped',`${track().title} - ${track().artist}`,{trackIndex:S.current});setFeedback(message,true);}catch(e){setFeedback(`Next track blocked. ${e.message||'Tap Play on receiver.'}`,false);}}if(push){S.command={id:uid(),type:'skip',createdAt:Date.now(),source:deviceLabel()};await pushState('Skip command sent to receivers.');}else if(S.screen==='home')await pushState('Receiver skip logged.');render();}
spotifyPlayUrl=async function(url=S.spotifyUrl,push=true){receiverActive=true;readMusicSettings();if(!S.spotifyUrl&&url)S.spotifyUrl=url;const playUrl=url||S.spotifyUrl;if(commandOnly(push)){if(!playUrl){setFeedback('Paste a Spotify playlist or song URL first.',false);render();return;}S.musicProvider='spotify';S.spotifyUrl=playUrl;await issueCommand('spotify-play',{label:'Play Spotify',url:playUrl,detail:compactUrl(playUrl)},'Spotify play command sent to receivers.');return;}if(!spotifyLoggedIn()){setFeedback('Spotify is not connected on this receiver. Use Login with Spotify on the Home receiver first.',false);render();return;}try{if(!S.spotifyDeviceId)await connectSpotifyReceiver().catch(()=>{});const device_id=await spotifyTargetDevice();if(!device_id)throw Error('No Spotify device found. Open Spotify on this receiver or tap Start Spotify Receiver.');await spotifyApi('PUT','/me/player/play',spotifyBody(playUrl),{device_id});await spotifySetVolume(S.spotifyVolume);S.musicProvider='spotify';S.intent='playing';logEvent('play','Spotify playing',compactUrl(playUrl),{provider:'spotify'});setFeedback('Spotify playback started on the receiver.',true);if(push){S.command={id:uid(),type:'spotify-play',url:playUrl,createdAt:Date.now(),source:deviceLabel()};await pushState('Spotify play command sent to Home receivers.');}else if(S.screen==='home')await pushState('Receiver Spotify play logged.');}catch(e){setFeedback(`Spotify play failed: ${e.message}`,false);}render();}
sendAnnouncement=async function(text,hold=false){const msg=tokens(text).trim();if(!msg){setFeedback('Nothing to announce. Type text first.',false);return;}const id=uid();S.announcement={id,text:msg,hold,createdAt:Date.now(),source:deviceLabel()};if(S.screen==='home')lastAnnouncementId=id;logEvent('announcement',hold?'Safety announcement sent':'Announcement sent',msg.slice(0,150));await pushState('Announcement command sent to all active Home receivers.');if(S.screen==='home')await speak(msg,hold);render();}
const v55Speak=speak;
speak=async function(text,hold=false){await v55Speak(text,hold);if(S.screen==='home'){logEvent('announcement',hold?'Safety announcement played':'Announcement played',tokens(text).slice(0,150));await pushState('Receiver announcement logged.');}}
playScheduleItem=async function(item){if(!item){setFeedback('No schedule item selected.',false);return;}if(item.type==='spotify'){if(!item.url){setFeedback('This schedule item has no Spotify URL.',false);return;}S.musicProvider='spotify';S.spotifyUrl=item.url;if(S.screen==='home'){await spotifyPlayUrl(item.url,false);}else await issueCommand('spotify-play',{label:item.label,url:item.url,detail:`Scheduled Spotify: ${compactUrl(item.url)}`},`Spotify schedule item sent: ${item.label}`);render();return;}if(item.type==='song'){const index=Math.max(0,Math.min(Number(item.trackIndex)||0,(S.tracks||[]).length-1));S.current=index;S.musicProvider='suno';if(S.screen==='home'){await play(false);}else await issueCommand('song',{label:item.label,trackIndex:index,provider:'suno',detail:`Scheduled Suno song: ${(S.tracks[index]||{}).title||'track'}`},`Suno song sent to receivers: ${(S.tracks[index]||{}).title||item.label}`);render();return;}if(item.type==='suno'){if(!item.url){setFeedback('This schedule item has no Suno URL.',false);return;}if(S.screen==='home'){S.musicProvider='suno';S.playlistUrl=item.url;await importSuno('Scheduled Suno URL imported.');await play(false);}else await issueCommand('suno',{label:item.label,url:item.url,detail:`Scheduled Suno URL: ${compactUrl(item.url)}`},`Suno item sent to receivers: ${item.label}`);render();return;}await sendAnnouncement(itemBody(item),!!item.hold);}
applyRemote=async function(newCommand,newAnnouncement){if(S.screen!=='home')return;if(newCommand&&S.command?.id&&S.command.id!==lastCommandId&&!speaking){lastCommandId=S.command.id;const c=S.command;if(c.provider)S.musicProvider=c.provider;if(c.url){if(c.type==='spotify-play')S.spotifyUrl=c.url;else S.playlistUrl=c.url;}if(Number.isFinite(Number(c.trackIndex)))S.current=Math.max(0,Math.min(Number(c.trackIndex)||0,(S.tracks||[]).length-1));logEvent('receiver','Command received',`${c.type}${c.label?': '+c.label:''}`,{commandType:c.type});if(c.type==='play')await play(false);if(c.type==='pause')await pause(false);if(c.type==='stop')await stop(false);if(c.type==='skip')await nextTrack(false);if(c.type==='spotify-play'){S.musicProvider='spotify';S.spotifyUrl=c.url||S.spotifyUrl;await spotifyPlayUrl(S.spotifyUrl,false);}if(c.type==='song'){S.musicProvider='suno';await play(false);}if(c.type==='suno'){S.musicProvider='suno';S.playlistUrl=c.url;await importSuno('Remote Suno URL imported.');await play(false);}if(c.type==='weather-check')await weather();}if(newAnnouncement&&S.announcement?.id&&S.announcement.id!==lastAnnouncementId){lastAnnouncementId=S.announcement.id;await speak(S.announcement.text,S.announcement.hold);}}
const v55Weather=weather;
weather=async function(){logEvent('weather',S.screen==='home'?'Weather check running':'Weather command running','');await v55Weather();if(S.screen==='home')await pushState('Receiver weather check logged.');}
function addSongSchedule(index){const t=S.tracks[Math.max(0,Math.min(Number(index)||0,(S.tracks||[]).length-1))]||{};const item={id:'sched-'+uid(),label:`Play ${t.title||'Suno song'}`,type:'song',time:'10:00',trackIndex:Number(index)||0,enabled:true};S.schedule.push(item);S.editId=item.id;S.tab='schedule';save(`Added ${item.label} to the daily schedule.`);}
function createScheduleItem(){const item={id:'sched-'+uid(),label:'New Schedule Item',type:'announcement',time:'10:00',announcementId:S.selected,enabled:true};S.schedule.push(item);S.editId=item.id;save('New schedule item added.');}
saveRow=function(i){const r=S.schedule[i];if(!r){setFeedback('Schedule item not found.',false);return;}const kind=document.querySelector(`[data-kind="${i}"]`)?.value||'announcement';const txt=document.querySelector(`[data-body="${i}"]`)?.value.trim()||'';r.label=document.querySelector(`[data-label="${i}"]`)?.value||r.label;r.time=document.querySelector(`[data-row-time="${i}"]`)?.value||r.time||'10:00';delete r.offsetToClose;delete r.announcementId;delete r.url;delete r.text;delete r.trackIndex;if(kind==='song'){r.type='song';r.trackIndex=Math.max(0,Math.min(Number(document.querySelector(`[data-track="${i}"]`)?.value)||0,(S.tracks||[]).length-1));}else if(kind==='suno'){r.type='suno';r.url=txt||S.playlistUrl;if(!r.url){setFeedback('Paste a Suno URL before saving.',false);return;}}else if(kind==='spotify'||/^spotify:|^https?:\/\/open\.spotify\.com\//i.test(txt)){r.type='spotify';r.url=txt||S.spotifyUrl;if(!r.url){setFeedback('Paste a Spotify URL before saving.',false);return;}}else if(kind==='text'){r.type='text';r.text=txt||'Type announcement text here.';}else{r.type='announcement';r.announcementId=document.querySelector(`[data-selann="${i}"]`)?.value||S.selected;}save(`Schedule item saved: ${r.label}.`);}
nav=function(){return `<nav class="tabs stickyTabs">${[['command','Command'],['music','Music'],['schedule','Schedule'],['weather','Weather'],['voice','Voice'],['hours','Hours']].map(x=>`<button data-tab="${x[0]}" class="${S.tab===x[0]?'on':''}">${x[1]}</button>`).join('')}</nav>`;}
shell=function(body){return `${header()}<main class="wrap commandWrap"><div class="version">Command Center · Version ${VERSION} · ${S.sync?'Sync Active':'Preview Sync'} · ${esc(S.syncMode||'')} · ${esc(deviceLabel())}</div>${nav()}<div id="feedbackBox" class="status"><b>Status:</b> ${esc(S.feedback||'Ready.')}</div>${S.lastError?`<div class="status warnBox"><b>Last Error:</b> ${esc(S.lastError)}</div>`:''}${body}</main>`;}
commandPage=function(){const selected=ann();return shell(`<section class="panel commandHero"><div><p class="tag">Live Control</p><h1>Command</h1><p>Control every active receiver from this phone. Receivers do the playing; command phones only send instructions.</p></div>${currentStatusCards()}</section><section class="panel controlPanel"><h2>Receiver Controls</h2><div class="bigControls"><button id="playCmd">Play</button><button id="pauseCmd" class="secondary">Pause</button><button id="skipCmd" class="secondary">Skip</button><button id="stopCmd" class="danger">Stop</button><button id="checkWeatherCmd" class="secondary">Check Weather</button></div><div class="grid2"><label>Quick Announcement<textarea id="quickText">${esc(S.quickText||selected.text)}</textarea></label><div><label>Saved Announcement<select id="quickTemplate">${S.anns.map(a=>`<option value="${a.id}" ${a.id===S.selected?'selected':''}>${esc(a.label)}</option>`).join('')}</select></label><div class="stackBtns"><button id="quickPlay">Speak Now</button><button id="quickHold" class="secondary">Speak as Safety Hold</button><button id="lightningNow" class="secondary">Lightning Hold</button><button id="windNow" class="secondary">Wind Umbrellas</button></div></div></div></section><section class="panel"><div class="sectionHead"><h2>Receiver Log</h2><button id="clearLog" class="secondary">Clear Local Log</button></div><div class="logList">${logRows()}</div></section>`);}
musicPage=function(){const providers=Object.entries(PROVIDERS).map(([id,label])=>`<option value="${id}" ${S.musicProvider===id?'selected':''}>${label}</option>`).join('');const q=S.tracks.map((x,i)=>`<div class="q trackRow ${i===S.current?'cur':''}"><div><b>${i+1}. ${esc(x.title)}</b><span>${esc(x.artist)} · ${esc(x.duration||'')} · ${x.audioUrl?'ready':'title only'}</span></div><div class="rowBtns"><button data-song="${i}">Play</button><button data-schedule-song="${i}" class="secondary">Schedule</button></div></div>`).join('');const spotifyStatus=spotifyLoggedIn()?'Connected on this receiver':'Not connected on this receiver';return shell(`<section class="panel setupPanel"><div class="sectionHead"><div><p class="tag">Music Setup</p><h1>Music</h1></div><button id="saveMusic">Save</button></div><div class="grid2"><label>Provider<select id="musicProvider">${providers}</select></label><label>Station Name<input id="playlistName" value="${esc(S.playlistName)}"></label></div><div class="grid2"><label>Suno Playlist URL<input id="playlistUrl" value="${esc(S.playlistUrl)}"></label><label>Spotify Playlist or Song URL<input id="spotifyUrl" value="${esc(S.spotifyUrl)}" placeholder="https://open.spotify.com/playlist/..."></label></div><div class="spotifyBox"><div class="grid2"><label>Spotify Client ID<input id="spotifyClientId" value="${esc(S.spotifyClientId)}" placeholder="From developer.spotify.com/dashboard"></label><label>Receiver Device<input id="spotifyDeviceName" value="${esc(S.spotifyDeviceName||S.spotifyDeviceId||'Auto')}" readonly></label></div><div class="grid2"><label>Music Volume<input id="spotifyVolume" inputmode="numeric" value="${esc(S.spotifyVolume)}"></label><label>Announcement Duck Volume<input id="spotifyDuckedVolume" inputmode="numeric" value="${esc(S.spotifyDuckedVolume)}"></label></div><div class="status"><b>Spotify:</b> ${esc(spotifyStatus)}<br><b>Redirect URI:</b> <code>${esc(spotifyRedirectUri())}</code></div></div><div class="actions"><button id="spotifyLogin" class="secondary">Login with Spotify</button><button id="spotifyReceiver" class="secondary">Start Receiver</button><button id="spotifyClear" class="secondary">Clear Spotify</button><button id="import" class="secondary">Import Suno</button></div></section><section class="panel"><div class="sectionHead"><h2>Suno Queue</h2><button id="playCmd" class="secondary">Play Receivers</button></div>${q}</section>`);}
schedulePage=function(){const opts=S.anns.map(a=>`<option value="${a.id}">${esc(a.label)}</option>`).join('');const rows=S.schedule.map((r,i)=>({r,i})).sort((a,b)=>mins(schedTime(a.r))-mins(schedTime(b.r))).map(({r,i})=>{const active=S.editId===r.id;const mode=r.type==='spotify'?'spotify':r.type==='song'?'song':r.type==='suno'?'suno':r.type==='text'?'text':'announcement';const edit=active?`<div class="editBox"><div class="grid3"><label>Time<input data-row-time="${i}" type="time" value="${schedTime(r)}"></label><label>Type<select data-kind="${i}"><option value="announcement" ${mode==='announcement'?'selected':''}>Saved announcement</option><option value="text" ${mode==='text'?'selected':''}>Custom announcement</option><option value="song" ${mode==='song'?'selected':''}>Suno song</option><option value="suno" ${mode==='suno'?'selected':''}>Suno playlist URL</option><option value="spotify" ${mode==='spotify'?'selected':''}>Spotify URL</option></select></label><label>Label<input data-label="${i}" value="${esc(r.label)}"></label></div><label>Text or URL<textarea data-body="${i}">${esc(r.url||r.text||'')}</textarea></label><div class="grid2"><label>Suno Song<select data-track="${i}">${trackOptions(r.trackIndex)}</select></label><label>Saved Announcement<select data-selann="${i}">${opts}</select></label></div><button data-save-row="${i}">Save Item</button></div>`:'';return `<div class="scheduleRow ${r.enabled?'':'off'} ${active?'editing':''}"><div><b>${pretty(schedTime(r))} · ${esc(r.label)}</b><span>${r.enabled?'enabled':'off'} · ${esc(mode)}</span><p>${esc(tokens(itemBody(r)))}</p>${edit}</div><div class="rowBtns"><button data-play-row="${i}">Play</button><button data-edit="${i}" class="${active?'':'secondary'}">${active?'Close':'Edit'}</button><button data-toggle="${i}" class="secondary">${r.enabled?'Off':'On'}</button><button data-delete="${i}" class="secondary">Delete</button></div></div>`;}).join('');return shell(`<section class="panel"><div class="sectionHead"><div><p class="tag">Daily Automation</p><h1>Schedule</h1></div><button id="addSched">Add Item</button></div><p>Mix announcements, specific Suno songs, Suno playlist URLs, and Spotify URLs at set times each day.</p>${rows}</section>`);}
weatherPage=function(){return shell(`<section class="panel"><div class="sectionHead"><div><p class="tag">Safety Automation</p><h1>Weather</h1></div><button id="checkWeather">Check Weather</button></div><label>Address<input id="address" value="${esc(S.address)}"></label><div class="actions"><button id="verify" class="secondary">Verify Address</button><button id="gps" class="secondary">Use Device GPS</button></div><div class="grid3"><label>Latitude<input id="lat" value="${esc(S.lat)}"></label><label>Longitude<input id="lon" value="${esc(S.lon)}"></label><label>Weather Radius Miles<input id="radius" value="${esc(S.radius)}"></label></div><div class="grid3"><label>Lightning Radius Miles<input id="lightningRadiusMiles" value="${esc(S.lightningRadiusMiles)}"></label><label>Lightning Hold Minutes<input id="lightningHoldMinutes" value="${esc(S.lightningHoldMinutes)}"></label><label>Strong Wind Alert MPH<input id="windGustMph" inputmode="numeric" value="${esc(S.windGustMph)}"></label></div><label>Auto Scan<select id="weatherAuto"><option value="true">Every 5 minutes on active Home receivers</option><option value="false">Manual only</option></select></label><label>Lightning Announcement<textarea id="lightningText">${esc(S.lightningText)}</textarea></label><label>Strong Wind Announcement<textarea id="windText">${esc(S.windText)}</textarea></label><label>Lightning All Clear Announcement<textarea id="lightningClearText">${esc(S.lightningClearText)}</textarea></label><button id="saveLoc">Save Weather Settings</button><div class="status"><b>Weather:</b> ${esc(S.weather)}<br><b>${esc(S.weatherCheckedAt?'Last checked '+S.weatherCheckedAt:'Not checked yet')}</b><br><b>Lightning:</b> ${esc(lightningRemainingText())}</div></section>`);}
voicePage=function(){const opts=bestVoices().map(v=>`<option value="${esc(v.name)}" ${v.name===S.deviceVoice?'selected':''}>${esc(v.name)} · ${esc(v.lang)}</option>`).join('');return shell(`<section class="panel"><div class="sectionHead"><div><p class="tag">Announcement Audio</p><h1>Voice</h1></div><button id="voiceHealth" class="secondary">Check Voice</button></div><p>AI voice uses OpenAI when the deployment has a valid key. Device voice is always available when the browser supports speech synthesis.</p><div class="status"><b>Voice health:</b> ${esc(S.voiceHealth)}</div><div class="grid2"><label>Voice Mode<select id="voiceMode"><option value="ai">AI first, device fallback</option><option value="device">Device only</option></select></label><label>AI Voice<select id="aiVoice"><option value="marin">Marin</option><option value="cedar">Cedar</option><option value="coral">Coral</option><option value="nova">Nova</option><option value="sage">Sage</option><option value="shimmer">Shimmer</option><option value="onyx">Onyx</option></select></label></div><label>Device Voice<select id="deviceVoice"><option value="">Best available</option>${opts}</select></label><div class="grid2"><label>Speed<input id="rate" type="range" min=".75" max="1.15" step=".01" value="${S.rate}"></label><label>Pitch<input id="pitch" type="range" min=".85" max="1.15" step=".01" value="${S.pitch}"></label></div><div class="actions"><button id="saveVoice">Save Voice</button><button id="testVoice" class="secondary">Test Selected Voice</button><button id="testDevice" class="secondary">Test Device Voice</button></div></section>`);}
homePage=function(){const x=track();const next=S.schedule.filter(x=>x.enabled).sort((a,b)=>mins(schedTime(a))-mins(schedTime(b))).slice(0,5).map(x=>`<p class="line"><b>${pretty(schedTime(x))}</b><span>${esc(x.label)}</span></p>`).join('');return `${header()}<main class="home"><section class="hero compact"><div><p class="tag">Receiver · V${VERSION}</p><h1>Poolside Pulse</h1><p class="sub">Leave this screen open on every receiver connected to speakers.</p><div class="actions"><button id="playHome">${S.intent==='playing'?'Pause':'Activate Receiver'}</button><button id="skipHome" class="secondary">Skip</button><button id="stopHome" class="secondary">Stop</button></div><p class="mini">${receiverActive?'Receiver active':'Tap once to unlock audio'} · ${providerLabel()} · weather every 5 minutes</p></div><aside><b>Weather</b><p>${esc(S.weather)}</p><small>${esc(S.weatherCheckedAt?'Last checked '+S.weatherCheckedAt:'Not checked yet')} · wind ${esc(S.windGustMph)} mph</small><br><br><b>Lightning</b><p>${esc(lightningRemainingText())}</p></aside></section><section class="now"><div class="art">SS<span>Pulse</span></div><div><p class="tag teal">${S.intent==='playing'?'Now Playing':S.intent==='paused'?'Paused':'Station Ready'}</p><h2>${esc(S.musicProvider==='spotify'?'Spotify Receiver':x.title)}</h2><p>${esc(S.musicProvider==='spotify'?compactUrl(S.spotifyUrl):`${x.artist} · ${x.duration}`)}</p><div class="bar"><i></i></div><p>${esc(S.playlistName)}</p><p class="mini dark">${esc(S.feedback||'')}</p>${S.lastError?`<p class="warn">${esc(S.lastError)}</p>`:''}</div></section><section class="cards"><div class="card"><h3>Next Scheduled</h3>${next||'<p>No enabled schedule items.</p>'}</div><div class="card"><h3>Recent Receiver Log</h3>${logRows(5)}</div></section></main>`;}
render=function(){try{document.title=`Serenity Shores Poolside Pulse V${VERSION}`;document.getElementById('app').innerHTML=S.screen==='home'?homePage():!S.admin?login():({command:commandPage,music:musicPage,schedule:schedulePage,hours:hoursPage,weather:weatherPage,voice:voicePage}[S.tab]||commandPage)();bind();}catch(e){document.getElementById('app').innerHTML=`<main class="wrap"><section class="panel"><h1>Poolside Pulse V${VERSION}</h1><p class="warn">Recovered from runtime error: ${esc(e.message)}</p><button onclick="localStorage.removeItem('${KEY}');location.reload()">Reset local data</button></section></main>`;}}
bind=function(){if($('home'))$('home').onclick=()=>{S.screen='home';localSave();render();};if($('cmd'))$('cmd').onclick=()=>{S.screen='command';localSave();render();};if($('login'))$('login').onclick=()=>{if(val('pin')===PIN){S.admin=true;S.screen='command';S.tab='command';setFeedback('Admin unlocked.',true);render();}else setFeedback('Wrong PIN.',false);};document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{S.tab=b.dataset.tab;localSave();render();});if($('quickTemplate'))$('quickTemplate').onchange=()=>{S.selected=val('quickTemplate');const a=ann();S.quickText=a.text;localSave();render();};if($('playHome'))$('playHome').onclick=()=>S.intent==='playing'?pause(true):play(true);if($('skipHome'))$('skipHome').onclick=()=>nextTrack(true);if($('stopHome'))$('stopHome').onclick=()=>stop(true);if($('musicProvider'))$('musicProvider').value=S.musicProvider;if($('saveMusic'))$('saveMusic').onclick=()=>{readMusicSettings();S.spotifyClientId=val('spotifyClientId')||S.spotifyClientId;S.spotifyVolume=Math.max(20,Math.min(100,Number(val('spotifyVolume'))||92));S.spotifyDuckedVolume=Math.max(0,Math.min(25,Number(val('spotifyDuckedVolume'))||6));save('Music settings saved.');};if($('spotifyLogin'))$('spotifyLogin').onclick=spotifyLogin;if($('spotifyReceiver'))$('spotifyReceiver').onclick=()=>connectSpotifyReceiver().then(()=>{logEvent('receiver','Spotify receiver ready',S.spotifyDeviceName||'');setFeedback('Spotify receiver started. Now press Play.',true);return pushState('Spotify receiver status logged.');}).catch(e=>setFeedback(`Spotify receiver failed: ${e.message}`,false));if($('spotifyClear'))$('spotifyClear').onclick=clearSpotifyToken;if($('import'))$('import').onclick=()=>{S.musicProvider='suno';importSuno();};if($('openProvider'))$('openProvider').onclick=()=>{readMusicSettings();openProvider(true);};if($('playCmd'))$('playCmd').onclick=()=>{readMusicSettings();play(true);};if($('pauseCmd'))$('pauseCmd').onclick=()=>pause(true);if($('skipCmd'))$('skipCmd').onclick=()=>nextTrack(true);if($('stopCmd'))$('stopCmd').onclick=()=>stop(true);if($('checkWeatherCmd'))$('checkWeatherCmd').onclick=triggerWeatherCheck;if($('checkWeather'))$('checkWeather').onclick=triggerWeatherCheck;if($('voiceHealth'))$('voiceHealth').onclick=checkVoiceHealth;if($('clearLog'))$('clearLog').onclick=()=>{S.activityLog=[];save('Activity log cleared.');};if($('lightningNow'))$('lightningNow').onclick=()=>sendAnnouncement(S.lightningText,true);if($('windNow'))$('windNow').onclick=()=>sendAnnouncement(S.windText,false);document.querySelectorAll('[data-song]').forEach(b=>b.onclick=()=>{S.current=Number(b.dataset.song);S.musicProvider='suno';play(true);});document.querySelectorAll('[data-schedule-song]').forEach(b=>b.onclick=()=>addSongSchedule(Number(b.dataset.scheduleSong)));document.querySelectorAll('[data-ann]').forEach(b=>b.onclick=()=>{const a=S.anns.find(x=>x.id===b.dataset.ann);if(a){S.selected=a.id;S.quickText=a.text;setFeedback(`Loaded template: ${a.label}`,true);localSave();render();}});if($('newAnn'))$('newAnn').onclick=()=>{const id='custom-'+uid();S.anns.push({id,label:'New Template',text:'Type announcement text here.'});S.selected=id;S.quickText='Type announcement text here.';save('New announcement template added.');};if($('saveAnn'))$('saveAnn').onclick=()=>{const a=ann();a.label=val('annLabel')||a.label;a.text=val('annText')||a.text;S.guestName=val('guestName');save('Announcement template saved.');};if($('deleteAnn'))$('deleteAnn').onclick=()=>{if(S.anns.length<2){setFeedback('At least one template must remain.',false);return;}S.anns=S.anns.filter(a=>a.id!==S.selected);S.selected=S.anns[0].id;save('Announcement template deleted.');};if($('quickPlay'))$('quickPlay').onclick=()=>{S.quickText=val('quickText');sendAnnouncement(S.quickText,false);};if($('quickHold'))$('quickHold').onclick=()=>{S.quickText=val('quickText');sendAnnouncement(S.quickText,true);};if($('quickSchedule'))$('quickSchedule').onclick=()=>{S.quickText=val('quickText');const it={id:'sched-'+uid(),label:'Custom Announcement',type:'text',time:'10:00',text:S.quickText,enabled:true};S.schedule.push(it);S.editId=it.id;save('Quick text added to daily schedule.');};if($('addSched'))$('addSched').onclick=createScheduleItem;document.querySelectorAll('[data-time]').forEach(input=>input.onchange=()=>{const r=S.schedule[Number(input.dataset.time)];delete r.offsetToClose;r.time=input.value;save(`Time updated for ${r.label}.`);});document.querySelectorAll('[data-play-row]').forEach(b=>b.onclick=()=>playScheduleItem(S.schedule[Number(b.dataset.playRow)]));document.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>{const r=S.schedule[Number(b.dataset.edit)];S.editId=S.editId===r.id?'new':r.id;localSave();render();});document.querySelectorAll('[data-toggle]').forEach(b=>b.onclick=()=>{const r=S.schedule[Number(b.dataset.toggle)];r.enabled=!r.enabled;save(`${r.label} turned ${r.enabled?'on':'off'}.`);});document.querySelectorAll('[data-delete]').forEach(b=>b.onclick=()=>{const r=S.schedule[Number(b.dataset.delete)];S.schedule.splice(Number(b.dataset.delete),1);S.editId='new';save(`${r.label} deleted from schedule.`);});document.querySelectorAll('[data-save-row]').forEach(b=>b.onclick=()=>saveRow(Number(b.dataset.saveRow)));document.querySelectorAll('[data-selann]').forEach(s=>{const r=S.schedule[Number(s.dataset.selann)];if(r?.announcementId)s.value=r.announcementId;});if($('playbackMode'))$('playbackMode').value=S.playbackMode;if($('autoStart'))$('autoStart').value=String(S.autoStart);if($('autoStop'))$('autoStop').value=String(S.autoStop);if($('saveHours'))$('saveHours').onclick=()=>{S.poolOpen=val('poolOpen');S.poolClose=val('poolClose');S.playbackMode=val('playbackMode')||'always';S.autoStart=val('autoStart')==='true';S.autoStop=val('autoStop')==='true';save('Playback automation saved.');};if($('verify'))$('verify').onclick=geocode;if($('gps'))$('gps').onclick=useGps;if($('weatherAuto'))$('weatherAuto').value=String(S.weatherAuto);if($('saveLoc'))$('saveLoc').onclick=()=>{S.address=val('address');S.lat=val('lat');S.lon=val('lon');S.radius=val('radius');S.windGustMph=Math.max(15,Math.min(80,Number(val('windGustMph'))||35));S.lightningRadiusMiles=Math.max(1,Math.min(25,Number(val('lightningRadiusMiles'))||10));S.lightningHoldMinutes=Math.max(5,Math.min(90,Number(val('lightningHoldMinutes'))||30));S.lightningText=val('lightningText')||S.lightningText;S.windText=val('windText')||S.windText;S.lightningClearText=val('lightningClearText')||S.lightningClearText;S.weatherAuto=val('weatherAuto')!=='false';save('Weather settings saved.');};if($('voiceMode'))$('voiceMode').value=S.voiceMode;if($('aiVoice'))$('aiVoice').value=S.aiVoice;if($('deviceVoice'))$('deviceVoice').value=S.deviceVoice;if($('saveVoice'))$('saveVoice').onclick=()=>{S.voiceMode=val('voiceMode');S.aiVoice=val('aiVoice');S.deviceVoice=val('deviceVoice');S.rate=Number(val('rate'))||.94;S.pitch=Number(val('pitch'))||1;save('Voice settings saved.');};if($('testVoice'))$('testVoice').onclick=()=>{S.voiceMode=val('voiceMode');S.aiVoice=val('aiVoice');S.deviceVoice=val('deviceVoice');speak(`This is Serenity Shores Poolside Pulse version ${VERSION}. The selected voice is being tested.`,false);};if($('testDevice'))$('testDevice').onclick=()=>{const old=S.voiceMode;S.voiceMode='device';S.deviceVoice=val('deviceVoice');speak(`This is the device voice fallback test for Poolside Pulse version ${VERSION}.`,false).then(()=>{S.voiceMode=old;localSave();});};}
completeSpotifyLogin();
function loadVoices(){voices='speechSynthesis'in window?speechSynthesis.getVoices():[];}
if('speechSynthesis'in window){speechSynthesis.onvoiceschanged=loadVoices;loadVoices();}
function weatherMonitor(){if(S.weatherAuto&&S.screen==='home'&&receiverActive&&!speaking)weather();}
render();pullState();setInterval(pullState,2000);setInterval(tick,10000);setInterval(weatherMonitor,300000);setTimeout(weatherMonitor,15000);
